# 临时文件和缓存
hongzhiyun/

# 公共资源文件夹（不提交到Git）
public/

# Claude Code 工作目录
.omc/
.claude/
.superpowers/

# Claude Code 私人配置（不提交到 git）
CLAUDE.md
PROJECT_MEMO.md

# Claude Code 本地设置（不提交）
.claude/settings.local.json

# Vite 缓存
.vite/
node_modules/.vite/

# Git hooks (保留配置但排除工作文件)
.husky/_
.husky/*.sh

# Playwright MCP 测试产物
.playwright-mcp/

# 浏览器验证截图（根目录临时产物，pest-*.png 等）
pest-*.png

# Trae AI 文档
.trae/

# 依赖
node_modules/

# 构建输出
dist/
assets/

# TypeScript 编译产物（2026-07-07 修正：之前 scripts/ 规则忽略整个目录，掩盖了这里需要单独 ignore 编译产物的事实）
*.d.ts
*.d.ts.map
*.js.map
# server/scripts/ 下的 .js 是 .ts 的 tsc 编译产物（项目用 tsx 跑 .ts，不需要 .js）
server/scripts/**/*.js

# 临时脚本
*.tmp
*.log
.gstack/

# 备份文件
*.backup
*.bak
src_backup_*/

# 计划文档
PLANS/

# 环境变量
.env
.env.local
.env.*.local

# 操作系统文件
.DS_Store
Thumbs.db

# 临时文件
*.tmp
*.log

# 文档（非系统运行必需）
docs/

# Electron 桌面端（不提交）
electron/

# 其他临时资料（不提交）
others/
temp_backup/
test-results/
slides-new/
deliverables/

# 参考数据（非运行时必需）
参考数据/
参考数据/

# 临时辅助脚本（仅根目录 scripts/，2026-07-07 修正：之前 scripts/ 会误忽略 server/scripts/db-migrations/）
/scripts/

# 工具/规范目录（不提交）
.codegraph/
.specify/
openspec/

# 数据库备份文件（不提交）
server/data/backups/
server/data/yuanxingtu_backup_*.db
server/data/yuanxingtu.db.before-*.db

# 后端脚本（不提交）
server/src/scripts/

# SQLite 工具（不提交）
sqlite3.exe

# TypeScript 构建缓存（不提交）
tsconfig.tsbuildinfo
*.db-shm
*.db-wal
server/data/yuanxingtu.db.backup-*
server/data/yuanxingtu.db.save.tmp
