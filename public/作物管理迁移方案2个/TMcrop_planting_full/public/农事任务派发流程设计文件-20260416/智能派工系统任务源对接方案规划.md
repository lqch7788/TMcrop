# 智能派工系统 - 任务源对接方案规划

## Context

### 问题背景
当前智能派工系统与农事任务、临时任务、巡查问题三个任务源的对接存在问题：
- **农事任务**：新建时必须选择执行人，任务直接进入 pending 状态
- **临时任务**：新建时必须选择执行人，任务直接进入 pending 状态
- **巡查问题**：通过问题派发页面派发给执行人

**核心矛盾**：现有流程中执行人在创建时就被指定，智能派工系统只是"事后推荐"，无法真正实现AI智能派工。

### 用户提出的三种方案
1. **方案A**：统一生成任务，不在新建时选择执行人，提交到智能派工系统由AI推荐
2. **方案B**：新建和问题派发时提交执行人，智能派工系统结合已选执行人给出更优建议
3. **方案C**：系统支持两种模式切换 - 人工派发模式 vs AI自动生成派发任务（需确认）

### 推荐方案：三模式并行 + 渐进式切换 + 智能监测规划

**核心理念**：
- 提供三种派发模式，管理者可根据场景和信任度灵活选择
- 支持渐进式切换：从"纯人工" → "AI辅助" → "AI主导"
- 保留人工最终决策权，AI始终是辅助角色
- **新增**：AI自动监测工单完成情况，每日/每月智能规划任务

**三种模式定义：**

| 模式 | 说明 | 适用场景 |
|------|------|----------|
| **纯人工模式** | 保持现有流程，AI不参与推荐和决策 | 对AI信任度不高、特殊任务 |
| **AI辅助模式** | 创建任务时显示AI推荐，可选择接受或更换 | 日常使用，AI辅助决策 |
| **AI全自动模式** | AI自动预测任务并推荐，管理者批量确认后派发 | 信任AI、追求效率、标准化任务 |

**新增智能监测功能：**

| 功能 | 说明 | 价值 |
|------|------|------|
| **每日工单监测** | 自动分析任务完成情况，识别未完成/提前/推迟 | 实时掌握执行状态 |
| **每日任务规划** | 每天早上7点前自动生成当日任务规划 | 减少管理人员工作负担 |
| **月度任务规划** | 根据生产计划和生长周期生成30天任务规划 | 提前预判资源需求 |
| **资源需求预测** | 提前预估物资、工具、人员需求 | 支持采购和用工计划 |

---

## 一、现状分析

### 1.1 当前派发流程

| 任务类型 | 创建时是否选执行人 | 执行人指定时机 | 智能派工作用 |
|---------|-----------------|--------------|-------------|
| 农事任务 | **必须选择** | 创建时立即指定 | 无法干预 |
| 临时任务 | **必须选择** | 创建时立即指定 | 无法干预 |
| 巡查问题 | **可选** | 问题派发页面指定 | 可推荐执行人 |

### 1.2 核心问题

1. **农事/临时任务**：创建时强制选择执行人，智能派工系统只能"事后诸葛亮"
2. **缺乏统一入口**：三个任务源分散管理，无法统一调度
3. **缺少AI介入时机**：AI无法在任务创建时提供推荐

---

## 二、解决方案：三模式并行架构

### 2.1 三种派工模式

**模式1：纯人工模式（传统模式）**
- 完全保持现有流程，AI不介入
- 新建任务时立即选择执行人
- 任务直接进入 pending 状态，等待执行人接受
- 适用于：对AI信任度不高、特殊任务、紧急任务

**模式2：AI辅助模式（推荐日常使用）**
- 新建任务时显示AI推荐执行人列表
- 可选择接受AI推荐或手动更换
- 管理者最终确认派发
- 一步完成创建和派发
- 适用于：日常农事任务、病虫害问题

**模式3：AI全自动模式（高效场景）**
- AI自动预测任务并推荐执行人
- 管理者批量确认后派发
- 置信度≥80%的任务可直接自动派发（可选）
- 适用于：标准化任务、高效率场景、周期性任务

### 2.2 模式配置系统

```typescript
// src/types/dispatch.ts

export type DispatchMode = 'manual' | 'ai_assisted' | 'ai_auto';

export interface DispatchModeConfig {
  mode: DispatchMode;

  // 模式配置
  manual: {
    enabled: boolean;
    // 纯人工模式：保持现有流程，不接入AI
  };

  ai_assisted: {
    enabled: boolean;
    showRecommendationOnCreate: boolean;  // 创建任务时是否显示AI推荐
    defaultSelectTopWorker: boolean;      // 是否默认选中AI推荐的第一名
    requireConfirmation: boolean;         // 是否需要管理者确认
  };

  ai_auto: {
    enabled: boolean;
    autoPredictTasks: boolean;            // 是否自动预测任务
    autoRecommendWorkers: boolean;        // 是否自动推荐执行人
    requireBatchConfirmation: boolean;     // 是否需要批量确认
    confidenceThreshold: number;          // 自动派发置信度阈值（默认80）
    notifyWorkers: boolean;               // 是否自动通知执行人
  };

  // 全局配置
  allowModeSwitch: boolean;               // 是否允许随时切换模式
  defaultMode: DispatchMode;              // 默认模式
}
```

### 2.3 模式切换策略

**策略1：按任务类型自动选择模式**

```typescript
const MODE_BY_TASK_TYPE: Record<string, DispatchMode> = {
  'irrigation': 'ai_auto',      // 灌溉任务 → 全自动
  'fertilization': 'ai_auto',   // 施肥任务 → 全自动
  'pest_control': 'ai_assisted',// 植保任务 → AI辅助
  'pruning': 'ai_assisted',     // 修剪任务 → AI辅助
  'harvest': 'ai_auto',         // 采收任务 → 全自动
  'equipment_repair': 'manual',  // 设备维修 → 纯人工
  'emergency': 'manual',         // 紧急任务 → 纯人工
};
```

**策略2：按置信度自动降级**

```typescript
function getEffectiveMode(config: DispatchModeConfig, confidenceScore: number): DispatchMode {
  if (config.mode === 'ai_auto') {
    if (confidenceScore >= config.ai_auto.confidenceThreshold) {
      return 'ai_auto';  // 高置信度 → 全自动
    } else {
      return 'ai_assisted';  // 低置信度 → 降级为AI辅助
    }
  }
  return config.mode;
}
```

**策略3：按时间段切换模式**

```typescript
function getModeByTimeOfDay(config: DispatchModeConfig): DispatchMode {
  const hour = new Date().getHours();

  if (hour >= 6 && hour < 10) {
    return 'ai_auto';  // 早晨 → 全自动（安排当日任务）
  } else if (hour >= 10 && hour < 16) {
    return 'ai_assisted';  // 白天 → AI辅助（处理临时任务）
  } else {
    return 'manual';  // 晚上 → 纯人工（非工作时间）
  }
}
```

### 2.4 任务状态扩展

**状态机完整流程**：

```
draft ──► pending_ai ──► recommended ──► pending
 │           │              │             │
 │    (待AI推荐)      (AI已推荐)    (已派发)
 │           │              │             │
 │           ▼              ▼             ▼
 │     pending_ai ──► recommended ──► pending ──► accepted
 │           │              │             │      │
 │           │              │             │      ▼
 │           └──────────────┴─────────────┘  in_progress
 │                                            │
 └────────────────────────────────────────────┼─►
                                               ▼
                                          completed
```

**状态定义**：

```typescript
// 任务派发状态
export type DispatchStatus =
  | 'draft'           // 草稿（未提交）
  | 'pending_ai'      // 待AI推荐（已提交，等待AI生成推荐）
  | 'recommended'     // AI已推荐（待管理者确认）
  | 'pending'         // 已派发（等待执行人接受）
  | 'accepted'        // 已接受
  | 'in_progress'     // 执行中
  | 'completed'       // 已完成
  | 'rejected';       // 已驳回

// 进度状态
export type ProgressStatus = 'on_track' | 'ahead' | 'delayed' | 'cancelled';
```

### 2.5 统一任务模型（完整定义）

```typescript
// 统一派发任务（扩展现有结构）
export interface UnifiedDispatchTask {
  // === 基础字段 ===
  id: string;
  taskName: string;
  taskType: string;
  sourceType: 'farm' | 'temp' | 'problem';
  sourceId?: string;

  // === 派发相关字段 ===
  dispatchStatus: DispatchStatus;      // 派发状态
  dispatchMode: DispatchMode;         // 派发模式

  // === 执行人相关 ===
  assignedTo?: string;                 // 执行人ID
  assignedAt?: string;                 // 分配时间
  originalAssigneeId?: string;        // 原始选择的执行人ID（模式A优化建议用）

  // === AI推荐相关 ===
  submitToAiAt?: string;              // 提交到AI的时间
  aiRecommendedAt?: string;           // AI推荐的时间
  aiRecommendedWorkers?: WorkerRecommendation[]; // AI推荐的员工列表
  aiConfidenceScore?: number;          // AI推荐置信度
  aiRecommendationAccepted?: boolean;  // 是否接受了AI推荐

  // === AI优化建议（模式A）===
  aiOptimizationSuggestion?: {
    suggestedWorkerId: string;
    suggestedWorkerName: string;
    confidenceScore: number;
    reason: string;
    originalWorkerId: string;
    originalWorkerName: string;
  };

  // === 预测任务相关 ===
  isPredictedTask: boolean;           // 是否为预测任务
  predictedBy?: string;               // 预测引擎标识
  predictedAt?: string;               // 预测时间

  // === 任务进度相关 ===
  plannedDate: string;                // 计划日期
  actualStartDate?: string;          // 实际开始日期
  actualCompletionDate?: string;      // 实际完成日期
  progressStatus: ProgressStatus;    // 进度状态
  progressPercentage: number;         // 进度百分比 0-100
  delayDays?: number;                // 延迟天数
  delayReason?: string;              // 延迟原因

  // === 规划相关 ===
  planningHorizon: 'daily' | 'weekly' | 'monthly';  // 规划周期
  planningDate: string;               // 规划生成日期
  planningBatch?: string;             // 关联的生产批次
  isAutoPlanned: boolean;             // 是否为AI自动规划
  planningNotes?: string;             // 规划备注

  // === 其他字段 ===
  priority: 'urgent' | 'high' | 'medium' | 'low';
  status: TaskStatus;
  createdAt: string;
  updatedAt: string;
}
```

### 2.3 优化建议机制

对于模式A（已选执行人）的任务，AI仍可提供优化建议：
- **负荷评估**：执行人当前负荷是否过重？
- **技能匹配度**：是否真正匹配任务需求？
- **时间冲突**：是否有更优执行人可用？

管理员可选择"接受AI建议更换执行人"或"保持原选择"。

---

## 三、流程设计

### 3.1 农事任务派发流程

```
创建农事任务
    │
    ├─► 选择执行人 ─► 保存派发 ─► pending ─► 执行人接受
    │
    └─► 选择"待智能推荐"
            │
            ▼
        pending_ai
            │
            ▼
    ┌─────────────────┐
    │  智能派工系统   │
    │  生成推荐列表   │
    └─────────────────┘
            │
            ▼
        recommended（显示AI推荐Top3）
            │
    ┌───────┴───────┐
    │  管理员确认    │
    ├─► 接受AI推荐 ─► pending ─► 执行人接受
    ├─► 手动选择 ─► pending ─► 执行人接受
    └─► 取消任务
```

### 3.2 临时任务派发流程

```
创建临时任务
    │
    ├─► 选择执行人 ─► 保存派发 ─► pending ─► 执行人接受
    │
    └─► 选择"待智能推荐"
            │
            ▼
        pending_ai
            │
            ▼
    ┌─────────────────┐
    │  智能派工系统   │
    │  生成推荐列表   │
    └─────────────────┘
            │
            ▼
        recommended
            │（同农事任务流程）
```

### 3.3 巡查问题派发流程

```
发现巡查问题
    │
    └─► 提交问题 ─► 待处理
                │
                ▼
        ┌─────────────────┐
        │  智能派工系统   │
        │  问题分析+推荐   │
        └─────────────────┘
                │
                ▼
            recommended（AI推荐执行人）
                │
    ┌───────┴───────┐
    │  管理员确认    │
    ├─► 接受AI推荐 ─► 创建关联任务 ─► pending ─► 执行人接受
    ├─► 手动选择 ─► 创建关联任务 ─► pending ─► 执行人接受
    └─► 转交其他部门
```

### 3.4 预测任务自动生成流程

```
任务预测引擎
    │
    ├─► 生长周期预测任务
    ├─► 环境告警触发任务（土壤湿度低→灌溉）
    └─► 病虫害预警任务
            │
            ▼
        进入"预测任务队列"
            │
            ▼
    ┌─────────────────┐
    │  智能派工系统   │
    │  自动匹配执行人  │
    │  置信度≥80%时   │
    │  自动派发       │
    │  置信度<80%时   │
    │  进入待确认队列  │
    └─────────────────┘
            │
            ▼
    ┌─────────────────┐
    │  管理员审核     │
    │  - 确认派发     │
    │  - 调整执行人   │
    │  - 延后处理     │
    └─────────────────┘
```

---

## 四、界面交互设计

### 4.1 新建任务页面 - 执行人选择

```
┌─────────────────────────────────────────────┐
│ 执行人选择                                   │
├─────────────────────────────────────────────┤
│ ○ 李建国（当前负荷85%）                      │ ← 快速选择
│ ○ 王建华（当前负荷60%）                      │
│ ○ 张三（当前负荷30%）                       │
│                                             │
│ ● 待智能推荐 ─► 提交到AI系统获取最佳推荐   │ ← 智能模式
│                                             │
│ [查看AI推荐理由]                             │
└─────────────────────────────────────────────┘
```

### 4.2 智能派工确认页面

```
┌─────────────────────────────────────────────────────┐
│ 智能派工确认                        [批量确认]      │
├─────────────────────────────────────────────────────┤
│ ☐ 全选                                              │
│                                                     │
│ ☑ 农事任务：番茄灌溉 - A区1号温室                   │
│   AI推荐：张三（置信度92%）                         │
│   理由：✅技能匹配95% ✅距离近 ✅当前空闲           │
│   [接受] [更换] [延后]                            │
│                                                     │
│ ☑ 临时任务：设备维修 - B区2号温室                  │
│   AI推荐：王建华（置信度78%）                       │
│   理由：✅技能匹配88% ⚠️距离较远                  │
│   [接受] [更换] [延后]                            │
│                                                     │
│ ☐ 巡查问题：叶片发黄 - C区1号温室                  │
│   AI推荐：李建国（置信度65%）                       │
│   理由：⚠️技能匹配仅55% ⚠️负荷较高              │
│   [接受] [更换] [延后] [忽略]                      │
└─────────────────────────────────────────────────────┘
```

### 4.3 优化建议弹窗（模式A任务）

```
┌─────────────────────────────────────────────────────┐
│ ⚠️ AI优化建议                                       │
├─────────────────────────────────────────────────────┤
│ 您选择的执行人李建国当前负荷较高（85%）              │
│                                                     │
│ AI发现更优执行人：                                  │
│                                                     │
│ 张三（置信度91%）                                   │
│ 理由：✅技能匹配95% ✅当前空闲 ✅距离近             │
│                                                     │
│ [接受AI建议更换为张三]                              │
│ [保持原选择李建国]                                  │
└─────────────────────────────────────────────────────┘
```

---

## 五、技术实现方案

### 5.1 类型扩展

**文件**: `src/types/dispatch.ts`

```typescript
// 任务派发状态
export type DispatchStatus =
  | 'draft'           // 草稿
  | 'pending_ai'      // 待AI推荐
  | 'recommended'      // AI已推荐
  | 'pending'          // 已派发（等待接受）
  | 'accepted'         // 已接受
  | 'in_progress'      // 执行中
  | 'completed';       // 已完成

// 任务派发模式
export type DispatchMode = 'fast' | 'ai_assisted';

// 统一派发任务（扩展现有结构）
export interface UnifiedDispatchTask {
  // ... 现有字段 ...
  dispatchStatus: DispatchStatus;     // 派发状态
  dispatchMode: DispatchMode;         // 派发模式
  submitToAiAt?: string;             // 提交到AI的时间
  aiRecommendedAt?: string;          // AI推荐的时间
  aiRecommendedWorkers?: WorkerRecommendation[]; // AI推荐的员工列表
  originalAssigneeId?: string;       // 原始选择的执行人ID（模式A）
}
```

### 5.2 Hook 扩展

**文件**: `src/hooks/useComprehensiveDispatch.ts`

```typescript
// 新增功能
export function useComprehensiveDispatch() {
  // ... 现有代码 ...

  // 提交任务到AI推荐队列
  const submitToAiRecommend = useCallback((
    task: UnifiedDispatchTask
  ) => {
    // 更新任务状态为 pending_ai
    // 记录提交时间
  }, []);

  // 确认AI推荐
  const confirmAiRecommend = useCallback((
    taskId: string,
    workerId: string
  ) => {
    // 更新任务状态为 pending
    // 设置执行人
  }, []);

  // 获取待确认的AI推荐任务
  const pendingConfirmTasks = useMemo(() => {
    return taskPool.filter(t =>
      t.dispatchStatus === 'recommended'
    );
  }, [taskPool]);

  // ... 其他代码 ...
}
```

### 5.3 任务源对接改造

**农事任务创建页面** (`TaskDispatchPage.tsx`)
- 执行人选择增加"待智能推荐"选项
- 选择该选项时，任务 dispatchMode = 'ai_assisted'

**临时任务创建页面** (`TempTaskPage.tsx`)
- 同样增加"待智能推荐"选项

**巡查问题派发页面** (`ProblemDispatchPage.tsx`)
- 默认提交到智能派工系统
- 管理员确认后派发

---

## 六、实施步骤（33天详细路线图）

### 阶段一：基础架构搭建（4-5天）

**Day 1-2: 类型和状态扩展**
- [ ] 扩展 `UnifiedDispatchTask` 添加派发状态和模式
- [ ] 创建 `DispatchStatus` 和 `DispatchMode` 类型
- [ ] 更新 `useTasks`、`useTempTasks`、`useProblemDispatch` 添加新状态处理
- [ ] 编写类型定义和单元测试

**Day 3-4: 模式配置系统**
- [ ] 创建 `useDispatchModeConfig` hook
- [ ] 实现三种模式定义和配置
- [ ] 实现模式切换逻辑
- [ ] 创建模式设置UI
- [ ] 编写单元测试

**Day 5: 统一任务创建Hook**
- [ ] 创建 `useUnifiedTaskCreation` hook
- [ ] 统一三种任务系统的创建接口
- [ ] 集成派发状态流转逻辑
- [ ] 编写集成测试

### 阶段二：每日/月度规划引擎（5-6天）

**Day 6-8: 每日工单汇总与分析**
- [ ] 创建 `useDailyWorkOrderAnalysis` hook
- [ ] 实现进度分析算法
- [ ] 实现人员负荷分析
- [ ] 实现AI建议生成
- [ ] 创建每日报告UI组件
- [ ] 编写集成测试

**Day 9-11: 每日任务规划**
- [ ] 创建 `useDailyTaskPlanning` hook
- [ ] 实现定时任务触发（模拟）
- [ ] 实现任务预测和人员匹配
- [ ] 实现派工计划生成
- [ ] 实现推送提醒功能
- [ ] 创建每日规划页面

### 阶段三：月度规划引擎（4-5天）

**Day 12-14: 月度规划算法**
- [ ] 创建 `useMonthlyTaskPlanning` hook
- [ ] 实现30天任务预测
- [ ] 实现按周/按日汇总
- [ ] 实现物资需求分析
- [ ] 实现工具需求分析
- [ ] 实现人员需求分析
- [ ] 实现成本预估

**Day 15-16: 月度规划UI**
- [ ] 创建月度规划页面
- [ ] 创建月度规划报告组件
- [ ] 实现任务日历视图
- [ ] 实现物资/工具/人员需求表格
- [ ] 实现导出功能

### 阶段四：AI推荐组件开发（3-4天）

**Day 17-18: AI推荐面板**
- [ ] 创建 `AIRecommendationPanel` 组件
- [ ] 实现推荐结果展示
- [ ] 实现选择交互
- [ ] 实现推荐理由展示
- [ ] 编写组件测试

**Day 19-20: AI优化建议机制**
- [ ] 创建 `useAIOptimization` hook
- [ ] 实现优化建议检测逻辑
- [ ] 创建优化建议弹窗组件
- [ ] 实现接受/拒绝建议交互
- [ ] 编写集成测试

### 阶段五：三个任务系统集成（4-5天）

**Day 21-22: 农事任务集成**
- [ ] 改造 `TaskFormModal` 组件（增加"待智能推荐"选项）
- [ ] 集成AI推荐面板
- [ ] 实现派发状态流转
- [ ] 测试完整流程

**Day 23-24: 临时任务集成**
- [ ] 改造 `TempTaskFormModal` 组件
- [ ] 集成AI推荐面板
- [ ] 实现紧急任务特殊处理
- [ ] 测试完整流程

**Day 25: 巡查反馈任务集成**
- [ ] 改造问题分派弹窗（默认AI推荐）
- [ ] 集成AI推荐面板
- [ ] 实现病虫害问题特殊处理
- [ ] 测试完整流程

### 阶段六：智能派工确认页面（3-4天）

**Day 26-27: 确认页面开发**
- [ ] 重构 `SmartDispatchPage.tsx`
- [ ] 实现待确认任务列表
- [ ] 实现按状态分组显示
- [ ] 实现单个任务操作（接受/更换/延后）
- [ ] 实现批量确认功能

**Day 28-29: 预测任务自动派发**
- [ ] 连接任务预测引擎
- [ ] 实现置信度≥80%自动派发
- [ ] 实现置信度<80%进入待确认队列
- [ ] 实现派发后状态更新

### 阶段七：测试优化（3-4天）

**Day 30-32: 全场景测试**
- [ ] 模式切换测试
- [ ] 状态流转测试
- [ ] 每日规划测试
- [ ] 月度规划测试
- [ ] AI推荐准确性测试
- [ ] 三种任务系统流程测试
- [ ] 边界场景测试
- [ ] 性能测试
- [ ] 修复Bug

**Day 33: 上线准备**
- [ ] 编写用户文档
- [ ] 准备培训材料
- [ ] 灰度发布计划

### 阶段六：智能监测与规划系统（5-6天）

**Day 22-23: 每日工单监测**
- [ ] 创建 `useTaskMonitoring` hook
- [ ] 实现任务完成状态分析逻辑
- [ ] 实现进度对比（未完成/提前/推迟）
- [ ] 创建工单监测展示组件

**Day 24-25: 每日任务规划**
- [ ] 创建定时任务调度系统
- [ ] 实现每日7点自动生成规划
- [ ] 创建今日任务规划展示页面
- [ ] 实现遗留任务处理逻辑

**Day 26-27: 月度任务规划**
- [ ] 创建 `useMonthlyPlanning` hook
- [ ] 实现生产计划到任务规划的转换
- [ ] 实现物资/工具/人员需求预测
- [ ] 创建月度规划展示页面

---

## 七、关键文件

### 基础派工模块

| 文件 | 说明 |
|------|------|
| `src/types/dispatch.ts` | 扩展派发状态和模式类型 |
| `src/hooks/useDispatchModeConfig.ts` | 模式配置Hook |
| `src/hooks/useUnifiedTaskCreation.ts` | 统一任务创建Hook |
| `src/hooks/useComprehensiveDispatch.ts` | 新增submitToAiRecommend、confirmAiRecommend等 |
| `src/components/dispatch/AIRecommendationPanel.tsx` | AI推荐面板组件 |
| `src/components/farm/taskDispatch/TaskDispatchPage.tsx` | 农事任务页面改造 |
| `src/components/labor/tempTask/TempTaskPage.tsx` | 临时任务页面改造 |
| `src/hooks/useProblemDispatch.ts` | 巡查问题派发改造 |
| `src/pages/SmartDispatch.tsx` | 智能派工确认页面 |

### 智能监测与规划模块

| 文件 | 说明 |
|------|------|
| `src/types/dispatch.ts` | 扩展派发状态和模式类型 |
| `src/types/planning.ts` | 每日/月度规划类型定义 |
| `src/hooks/useDispatchModeConfig.ts` | 模式配置管理 |
| `src/hooks/useUnifiedTaskCreation.ts` | 统一任务创建入口 |
| `src/hooks/useAIOptimization.ts` | AI优化建议检测 |
| `src/hooks/useDailyWorkOrderAnalysis.ts` | 每日工单汇总分析 |
| `src/hooks/useDailyTaskPlanning.ts` | 每日任务规划 |
| `src/hooks/useMonthlyTaskPlanning.ts` | 月度任务规划 |
| `src/hooks/useScheduledTasks.ts` | 定时任务管理 |
| `src/components/dispatch/AIRecommendationPanel.tsx` | AI推荐面板组件 |
| `src/components/dispatch/OptimizationSuggestionModal.tsx` | 优化建议弹窗 |
| `src/components/planning/DailyWorkOrderReport.tsx` | 每日工单报告 |
| `src/components/planning/MonthlyPlanReport.tsx` | 月度规划报告 |
| `src/pages/SmartDispatch.tsx` | 智能派工确认页面 |
| `src/pages/DailyPlanningPage.tsx` | 每日规划页面 |
| `src/pages/MonthlyPlanningPage.tsx` | 月度规划页面 |

---

## 八、技术实现代码

### 8.1 统一任务创建Hook

```typescript
// src/hooks/useUnifiedTaskCreation.ts

interface UnifiedTaskInput {
  // 通用字段
  taskName: string;
  taskType: string;
  greenhouseName: string;
  scheduledDate: string;
  estimatedHours: number;
  priority: 'urgent' | 'high' | 'medium' | 'low';

  // 来源标识
  sourceType: 'farm' | 'temp' | 'problem';
  sourceId?: string;

  // AI推荐相关
  useAIRecommendation: boolean;
  aiRecommendedWorkerId?: string;
  aiConfidenceScore?: number;

  // 农事任务特有字段
  batchId?: string;
  operationType?: FarmOperationType;

  // 临时任务特有字段
  isEmergency?: boolean;
  description?: string;

  // 巡查反馈特有字段
  problemId?: number;
  requireFeedback?: boolean;
  deadline?: string;
}

interface UnifiedTaskCreationReturn {
  createTask: (input: UnifiedTaskInput) => Promise<Task>;
  getAIRecommendation: (taskInfo: Partial<UnifiedTaskInput>) => Promise<WorkerMatchScore[]>;
  modeConfig: DispatchModeConfig;
  currentMode: DispatchMode;
  switchMode: (mode: DispatchMode) => void;
}

export function useUnifiedTaskCreation(): UnifiedTaskCreationReturn {
  const { createTask: createFarmTask } = useTasks();
  const { addTempTask } = useTempTasks();
  const { dispatchProblem } = useProblemDispatch();
  const { modeConfig, currentMode, switchMode } = useDispatchModeConfig();
  const { factors } = useDispatchFactors();

  const getAIRecommendation = async (taskInfo: Partial<UnifiedTaskInput>): Promise<WorkerMatchScore[]> => {
    const predictedTask: PredictedTask = { /* 根据taskInfo构建 */ };
    const matches = matchWorkers(predictedTask, factors.workers, factors.currentTasks, factors);
    return matches.slice(0, 3);
  };

  const createTask = async (input: UnifiedTaskInput): Promise<Task> => {
    switch (input.sourceType) {
      case 'farm':
        return await createFarmTask({ /* ... */ });
      case 'temp':
        return await addTempTask({ /* ... */ });
      case 'problem':
        return await dispatchProblem({ /* ... */ });
      default:
        throw new Error('Unknown task source type');
    }
  };

  return { createTask, getAIRecommendation, modeConfig, currentMode, switchMode };
}
```

### 8.2 AI推荐面板组件

```typescript
// src/components/dispatch/AIRecommendationPanel.tsx

interface AIRecommendationPanelProps {
  taskInfo: Partial<UnifiedTaskInput>;
  onWorkerSelect: (workerId: string, score: number) => void;
  onReRecommend?: () => void;
  onManualSelect?: () => void;
  config: AIRecommendConfig;
}

export function AIRecommendationPanel({
  taskInfo,
  onWorkerSelect,
  onReRecommend,
  onManualSelect,
  config,
}: AIRecommendationPanelProps) {
  const [recommendations, setRecommendations] = useState<WorkerMatchScore[]>([]);
  const [selectedWorkerId, setSelectedWorkerId] = useState<string | null>(null);
  const [isLoading, setIsLoading] = useState(false);

  const { getAIRecommendation } = useUnifiedTaskCreation();

  useEffect(() => { loadRecommendations(); }, [taskInfo]);

  const loadRecommendations = async () => {
    setIsLoading(true);
    try {
      const recs = await getAIRecommendation(taskInfo);
      setRecommendations(recs);
      if (config.autoSelectTop && recs.length > 0) {
        setSelectedWorkerId(recs[0].staffId);
        onWorkerSelect(recs[0].staffId, recs[0].totalScore);
      }
    } finally {
      setIsLoading(false);
    }
  };

  return (
    <div className="ai-recommendation-panel">
      <div className="panel-header">
        <span>🤖 AI推荐结果</span>
        <div className="actions">
          <Button icon="🔄" onClick={loadRecommendations} loading={isLoading}>重新推荐</Button>
          {onManualSelect && <Button icon="✏️" onClick={onManualSelect}>手动选择</Button>}
        </div>
      </div>
      {/* 推荐列表渲染 */}
    </div>
  );
}
```

---

## 九、风险与应对

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| 推荐算法响应慢（>3秒） | 用户体验差 | 性能优化，缓存推荐结果，懒加载 |
| 模式切换逻辑复杂 | 开发和维护成本高 | 清晰的模式定义，完善的单元测试 |
| 管理者不信任AI推荐 | 采纳率低 | 强调辅助角色，展示推荐理由，允许手动覆盖 |
| 全自动模式错误派发 | 任务分配不合理 | 高置信度阈值，完善的日志和撤销机制 |
| 三种任务系统接口不一致 | 集成困难 | 统一创建接口，适配器模式 |

---

## 十、验证方案

### 10.1 农事任务测试
1. [ ] 新建农事任务，选择"待智能推荐"
2. [ ] 任务出现在智能派工确认页面
3. [ ] 确认推荐后任务派发成功

### 10.2 临时任务测试
1. [ ] 新建临时任务，选择"待智能推荐"
2. [ ] 任务出现在智能派工确认页面
3. [ ] 确认推荐后任务派发成功

### 10.3 巡查问题测试
1. [ ] 提交新巡查问题
2. [ ] 问题出现在智能派工确认页面
3. [ ] 确认推荐后创建关联任务

### 10.4 优化建议测试
1. [ ] 新建任务选择执行人A
2. [ ] AI发现执行人B更优，弹出优化建议
3. [ ] 接受建议后执行人变为B

### 10.5 预测任务测试
1. [ ] 触发环境告警（土壤湿度低）
2. [ ] 自动生成灌溉任务
3. [ ] AI自动派发给最优执行人（置信度≥80%）

### 10.6 模式切换测试
1. [ ] 纯人工模式：任务直接派发，无AI介入
2. [ ] AI辅助模式：创建时显示AI推荐
3. [ ] AI全自动模式：自动预测任务，批量确认

### 10.7 每日工单监测测试
1. [ ] 查看今日任务完成状态统计
2. [ ] 验证"未完成"任务识别正确
3. [ ] 验证"提前完成"任务识别正确
4. [ ] 验证"推迟完成"任务识别正确
5. [ ] 查看任务完成详情和原因分析

### 10.8 每日任务规划测试
1. [ ] 早上7点自动生成任务规划
2. [ ] 遗留任务正确显示并提供处理建议
3. [ ] 今日计划任务正确显示
4. [ ] 巡查发现任务正确显示
5. [ ] 人力负荷预估准确
6. [ ] 一键确认全部派发功能正常

### 10.9 月度任务规划测试
1. [ ] 生产计划创建后自动生成月度任务
2. [ ] 任务量统计准确（按类型）
3. [ ] 物资需求预估准确
4. [ ] 人员需求预估准确
5. [ ] 任务日历显示正确
6. [ ] 导出月度规划功能正常

### 10.10 资源需求预测测试
1. [ ] 物资库存不足预警正确
2. [ ] 工具保养提醒正确
3. [ ] 人员短缺预警正确
4. [ ] 采购建议清单生成正确

### 10.11 状态流转测试
1. [ ] draft → pending_ai → recommended → pending → accepted → in_progress → completed
2. [ ] 各状态切换的UI表现正确
3. [ ] 状态变更后数据同步正确

### 10.12 AI优化建议测试
1. [ ] 纯人工模式下，AI后台检测更优执行人
2. [ ] 优化建议弹窗正确显示
3. [ ] 接受建议后执行人变更
4. [ ] 拒绝建议后执行人保持不变

---

## 十一、AI智能任务监测与规划系统

### 11.1 功能概述

**核心价值**：
- 自动监测每日工单完成情况，与计划对比分析
- 每天早上7点前自动生成当日任务规划
- 根据生产计划和作物生长周期，自动生成月度任务规划
- 提前预估物资、工具、人员需求

### 11.2 每日工单监测分析

**数据采集**：
```
┌─────────────────────────────────────────────────────────────┐
│                     每日工单汇总分析                          │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│ 📊 今日概况（2026-04-23）                                    │
│ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐        │
│ │ 📋 计划任务  │ │ ✅ 已完成    │ │ ⏰ 进行中    │        │
│ │    15        │ │    12        │ │     2        │        │
│ └──────────────┘ └──────────────┘ └──────────────┘        │
│                                                              │
│ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐        │
│ │ ❌ 未完成    │ │ 🟢 提前完成  │ │ 🟡 推迟完成  │        │
│ │     1        │ │     3        │ │     2        │        │
│ └──────────────┘ └──────────────┘ └──────────────┘        │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

**进度分析维度**：

| 分析类型 | 判断条件 | 说明 |
|---------|---------|------|
| **未完成** | 计划时间已过，状态≠已完成 | 需要追派或延期处理 |
| **提前完成** | 实际完成时间 < 计划完成时间 | 效率高于预期，可增加任务量 |
| **按时完成** | 实际完成时间 ≈ 计划完成时间 | 正常执行 |
| **推迟完成** | 实际完成时间 > 计划完成时间 | 存在延期因素，需分析原因 |

### 11.3 每日任务规划（早晨7点自动生成）

**触发机制**：
- 定时任务：每天早上6:30自动运行
- 手动触发：管理员可随时点击"重新分析"

**生成内容**：

```
┌─────────────────────────────────────────────────────────────┐
│ 📅 今日任务规划（2026-04-24）                     06:30生成 │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│ ⚠️ 待处理遗留（3项）                                         │
│ ┌────────────────────────────────────────────────────────┐ │
│ │ 🟡 [推迟] A区番茄灌溉 - 原计划4月22日                   │ │
│ │    推迟原因：执行人临时请假                             │ │
│ │    建议：重新派发给王建国，优先级提升为紧急            │ │
│ │    [立即派发] [延后一天] [忽略]                        │ │
│ └────────────────────────────────────────────────────────┘ │
│                                                              │
│ 📋 今日计划任务（12项）                                      │
│ ┌────────────────────────────────────────────────────────┐ │
│ │ 🟢 A区黄瓜 - 开花期 - 植保任务                         │ │
│ │    批次：SC20260402-003  优先级：高                     │ │
│ │    建议执行人：张三（AI推荐92分）                       │ │
│ │    所需物资：多菌灵2瓶、喷雾器                          │ │
│ │    [确认派发] [更换执行人] [延后]                      │ │
│ └────────────────────────────────────────────────────────┘ │
│                                                              │
│ 🆕 巡查发现（2项）                                          │
│ ┌────────────────────────────────────────────────────────┐ │
│ │ 🔴 A区番茄发现灰霉病早期症状                           │ │
│ │    来源：巡查员李四  发现时间：4月23日15:30            │ │
│ │    建议执行人：萧峰（AI推荐88分）有病害处理经验        │ │
│ │    所需物资：多菌灵、甲基托布津                        │ │
│ │    [立即派发] [延后] [忽略]                          │ │
│ └────────────────────────────────────────────────────────┘ │
│                                                              │
│ 📊 今日人力负荷预估                                         │
│ ┌────────────────────────────────────────────────────────┐ │
│ │ 张三：3个任务（正常）    王建国：2个任务（空闲）       │ │
│ │ 萧峰：4个任务（较忙）    李四：1个任务（空闲）        │ │
│ └────────────────────────────────────────────────────────┘ │
│                                                              │
│ [✅ 确认全部派发] [📥 导出今日计划] [⚙️ 调整配置]        │
└─────────────────────────────────────────────────────────────┘
```

### 11.4 月度任务规划（生产计划驱动）

**触发时机**：
- 生产计划创建/修改时自动触发
- 每月25日自动生成下月规划
- 管理员手动触发

**生成逻辑**：

```
生产计划创建
    │
    ▼
读取作物信息（品种、种植日期、预计采收期）
    │
    ▼
匹配生长周期任务规则库
    │
    ├─► 苗期：播种、浇水、温湿度管理
    ├─► 生长期：施肥、除草、病虫害防治
    ├─► 开花期：人工授粉、营养补充
    ├─► 结果期：灌溉、套袋、病虫害防控
    └─► 采收期：采收、分级、包装
    │
    ▼
计算每个任务的时间窗口
    │
    ▼
整合历史任务执行数据（完成率、耗时）
    │
    ▼
生成月度任务日历
```

**月度规划展示**：

```
┌─────────────────────────────────────────────────────────────┐
│ 📅 2026年5月任务规划预览                         30天      │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│ 📊 任务量统计                                                │
│ ┌────────────────────────────────────────────────────────┐ │
│ │ 灌溉任务：45次    施肥任务：15次    植保任务：12次     │ │
│ │ 巡查任务：60次    采收任务：8次     其他任务：10次     │ │
│ └────────────────────────────────────────────────────────┘ │
│                                                              │
│ 📦 物资需求预估                                             │
│ ┌────────────────────────────────────────────────────────┐ │
│ │ 复合肥：150kg     尿素：80kg       多菌灵：25瓶       │ │
│ │ 吡虫啉：20瓶      喷雾器：需维修2台                    │ │
│ └────────────────────────────────────────────────────────┘ │
│                                                              │
│ 👷 人员需求预估                                             │
│ ┌────────────────────────────────────────────────────────┐ │
│ │ 高峰期（5月10-15日）：需要8人                          │ │
│ │ 日常期：需要5人                                        │ │
│ │ 建议：5月上旬增加临时工3人                             │ │
│ └────────────────────────────────────────────────────────┘ │
│                                                              │
│ 📅 任务日历（点击查看详情）                                  │
│ ┌────────────────────────────────────────────────────────┐ │
│ │ 5/1  5/5  5/10  5/15  5/20  5/25  5/30              │ │
│ │  ──────●──────●──────●──────●──────●──────           │ │
│ │  灌溉   施肥   植保   采收   施肥   植保   ...         │ │
│ └────────────────────────────────────────────────────────┘ │
│                                                              │
│ [📥 导出月度规划] [🖨️ 打印物资清单] [✅ 确认规划]        │
└─────────────────────────────────────────────────────────────┘
```

### 11.5 每日工单汇总分析Hook

```typescript
// src/hooks/useDailyWorkOrderAnalysis.ts

interface TaskProgressAnalysis {
  taskId: string;
  taskName: string;
  plannedDate: string;
  actualCompletionDate?: string;
  progressStatus: 'on_track' | 'ahead' | 'delayed' | 'cancelled';
  delayDays?: number;
  delayReason?: string;
  originalAssignee?: string;
  actualAssignee?: string;
}

interface DailyWorkOrderReport {
  date: string;
  totalTasks: number;
  pendingTasks: number;
  inProgressTasks: number;
  completedTasks: number;
  overdueTasks: number;

  // 进度分析
  aheadTasks: TaskProgressAnalysis[];
  onTrackTasks: TaskProgressAnalysis[];
  delayedTasks: TaskProgressAnalysis[];
  unfinishedTasks: TaskProgressAnalysis[];

  // 人员分析
  workerLoadAnalysis: WorkerLoadAnalysis[];

  // AI建议
  aiRecommendations: string[];

  // 天气
  weatherForecast: WeatherData;
}

export function useDailyWorkOrderAnalysis() {
  const { tasks } = useTasks();
  const { tempTasks } = useTempTasks();
  const { factors } = useDispatchFactors();

  // 生成每日工单汇总报告
  const generateDailyReport = useCallback((date: string): DailyWorkOrderReport => {
    const todayTasks = getAllTasksForDate(date);
    const completedTasks = todayTasks.filter(t => t.status === 'completed');
    const inProgressTasks = todayTasks.filter(t => t.status === 'in_progress');
    const pendingTasks = todayTasks.filter(t => t.status === 'pending');
    const overdueTasks = todayTasks.filter(t => isOverdue(t, date));

    // 进度分析
    const aheadTasks = analyzeAheadTasks(todayTasks, date);
    const onTrackTasks = analyzeOnTrackTasks(todayTasks, date);
    const delayedTasks = analyzeDelayedTasks(todayTasks, date);
    const unfinishedTasks = analyzeUnfinishedTasks(todayTasks, date);

    // 人员负荷分析
    const workerLoadAnalysis = analyzeWorkerLoad(date);

    // AI建议生成
    const aiRecommendations = generateAIRecommendations({
      overdueTasks,
      delayedTasks,
      workerLoadAnalysis,
      weather: factors.weatherForecast,
    });

    return {
      date,
      totalTasks: todayTasks.length,
      pendingTasks: pendingTasks.length,
      inProgressTasks: inProgressTasks.length,
      completedTasks: completedTasks.length,
      overdueTasks: overdueTasks.length,
      aheadTasks,
      onTrackTasks,
      delayedTasks,
      unfinishedTasks,
      workerLoadAnalysis,
      aiRecommendations,
      weatherForecast: factors.weatherForecast,
    };
  }, [tasks, tempTasks, factors]);

  // 分析提前完成任务
  const analyzeAheadTasks = (tasks: UnifiedDispatchTask[], date: string): TaskProgressAnalysis[] => {
    return tasks
      .filter(t => t.status === 'completed' && t.actualCompletionDate)
      .filter(t => {
        const planned = new Date(t.plannedDate);
        const actual = new Date(t.actualCompletionDate!);
        return actual < planned;
      })
      .map(t => ({
        taskId: t.id,
        taskName: t.taskName,
        plannedDate: t.plannedDate,
        actualCompletionDate: t.actualCompletionDate,
        progressStatus: 'ahead' as const,
        delayDays: calculateDaysDiff(t.actualCompletionDate!, t.plannedDate),
        originalAssignee: t.originalAssigneeId,
        actualAssignee: t.assignedTo,
      }));
  };

  // 分析推迟任务
  const analyzeDelayedTasks = (tasks: UnifiedDispatchTask[], date: string): TaskProgressAnalysis[] => {
    return tasks
      .filter(t => t.progressStatus === 'delayed')
      .map(t => ({
        taskId: t.id,
        taskName: t.taskName,
        plannedDate: t.plannedDate,
        progressStatus: 'delayed' as const,
        delayDays: t.delayDays,
        delayReason: t.delayReason,
        originalAssignee: t.originalAssigneeId,
        actualAssignee: t.assignedTo,
      }));
  };

  // 分析未完成任务
  const analyzeUnfinishedTasks = (tasks: UnifiedDispatchTask[], date: string): TaskProgressAnalysis[] => {
    return tasks
      .filter(t => isOverdue(t, date) && t.status !== 'completed')
      .map(t => ({
        taskId: t.id,
        taskName: t.taskName,
        plannedDate: t.plannedDate,
        progressStatus: 'delayed' as const,
        delayDays: calculateDaysDiff(date, t.plannedDate),
        delayReason: t.delayReason || '未完成',
        originalAssignee: t.originalAssigneeId,
        actualAssignee: t.assignedTo,
      }));
  };

  // 人员负荷分析
  const analyzeWorkerLoad = (date: string): WorkerLoadAnalysis[] => {
    return factors.workers.map(worker => {
      const todayTasks = getTasksForWorker(worker.staffId, date);
      const completedTasks = todayTasks.filter(t => t.status === 'completed');
      const completionRate = todayTasks.length > 0
        ? (completedTasks.length / todayTasks.length) * 100
        : 0;

      return {
        workerId: worker.staffId,
        workerName: worker.name,
        todayTasks: todayTasks.length,
        completedTasks: completedTasks.length,
        completionRate,
        loadStatus: getLoadStatus(todayTasks.length),
        availability: todayTasks.length < 2 ? 'available' : 'busy',
      };
    });
  };

  // 生成AI建议
  const generateAIRecommendations = (context: {
    overdueTasks: UnifiedDispatchTask[];
    delayedTasks: TaskProgressAnalysis[];
    workerLoadAnalysis: WorkerLoadAnalysis[];
    weather: WeatherData;
  }): string[] => {
    const recommendations: string[] = [];

    // 超期任务建议
    if (context.overdueTasks.length > 0) {
      recommendations.push(
        `优先处理${context.overdueTasks.length}项超期任务：${context.overdueTasks.map(t => t.taskName).join('、')}`
      );
    }

    // 人员建议
    const availableWorkers = context.workerLoadAnalysis.filter(w => w.availability === 'available');
    if (availableWorkers.length > 0) {
      recommendations.push(
        `建议安排人员：${availableWorkers.map(w => w.workerName).join('、')}（当前空闲，可安排新任务）`
      );
    }

    // 天气建议
    if (context.weather.forecast?.includes('雨')) {
      recommendations.push('今日有降雨，户外作业建议调整到明日');
    }

    return recommendations;
  };

  return {
    generateDailyReport,
    analyzeAheadTasks,
    analyzeDelayedTasks,
    analyzeUnfinishedTasks,
    analyzeWorkerLoad,
  };
}
```

### 11.6 月度任务规划Hook

```typescript
// src/hooks/useMonthlyTaskPlanning.ts

interface MonthlyPlan {
  month: string;
  batches: string[];
  totalTasks: number;
  totalHours: number;
  totalCost: number;
  weeklySummaries: WeeklySummary[];
  taskTypeBreakdown: Record<string, number>;
  dailyPlans: Record<string, DailyPlan>;
  materialRequirements: MaterialRequirement[];
  toolRequirements: ToolRequirement[];
  workerRequirements: WorkerRequirement[];
  costBreakdown: CostBreakdown;
  generatedAt: string;
  generatedBy: string;
  planningHorizon: 'monthly';
}

export function useMonthlyTaskPlanning() {
  const { factors } = useDispatchFactors();
  const { predictTasks } = useTaskPrediction();

  const generateMonthlyPlan = useCallback(async (
    month: string,
    batchIds: string[]
  ): Promise<MonthlyPlan> => {
    const startDate = `${month}-01`;
    const endDate = getMonthEndDate(month);

    // 生成未来30天任务预测
    const allTasks: PredictedTask[] = [];
    let currentDate = new Date(startDate);
    while (currentDate <= new Date(endDate)) {
      const dateStr = currentDate.toISOString().split('T')[0];
      const dailyTasks = await predictTasks(dateStr);
      allTasks.push(...dailyTasks);
      currentDate.setDate(currentDate.getDate() + 1);
    }

    // 按周汇总
    const weeklySummaries = aggregateByWeek(allTasks, startDate, endDate);

    // 物资需求分析
    const materialRequirements = analyzeMaterialRequirements(allTasks);

    return {
      month,
      batches: batchIds,
      totalTasks: allTasks.length,
      totalHours: allTasks.reduce((sum, t) => sum + t.estimatedHours, 0),
      totalCost: 0,
      weeklySummaries,
      taskTypeBreakdown: {},
      dailyPlans: {},
      materialRequirements,
      toolRequirements: [],
      workerRequirements: [],
      costBreakdown: { materialCost: 0, toolCost: 0, laborCost: 0, total: 0 },
      generatedAt: new Date().toISOString(),
      generatedBy: 'AI Planning Engine',
      planningHorizon: 'monthly',
    };
  }, [factors, predictTasks]);

  return { generateMonthlyPlan };
}
```

### 11.7 定时任务配置

```typescript
// src/services/ScheduledTasks.ts

// 每天早上6:00运行每日规划
registerCronJob('daily-planning', '0 6 * * *', async () => {
  const { generateDailyPlan } = useDailyTaskPlanning();
  const today = new Date().toISOString().split('T')[0];
  const plan = await generateDailyPlan(today);
  await saveDailyPlan(plan);

  // 7:00推送提醒
  setTimeout(() => {
    sendNotificationToManagers({
      type: 'daily_plan_ready',
      title: '今日派工计划已生成',
      body: `今日共有${plan.totalTasks}项任务`,
    });
  }, 3600000);
});

// 生产计划创建后运行月度规划
export function onProductionPlanCreated(batchIds: string[]) {
  const { generateMonthlyPlan } = useMonthlyTaskPlanning();
  const month = new Date().toISOString().slice(0, 7);
  generateMonthlyPlan(month, batchIds).then(saveMonthlyPlan);
}

// 本地环境模拟定时任务
export function useScheduledTasks() {
  useEffect(() => {
    const dailyInterval = setInterval(() => {
      const now = new Date();
      if (now.getHours() === 6 && now.getMinutes() === 0) {
        generateDailyPlan();
      }
    }, 60000);
    return () => clearInterval(dailyInterval);
  }, []);
}
```

### 11.8 物资工具人员需求预测

**预测模型**：

```typescript
// 需求预测接口
interface ResourceForecast {
  // 物资需求
  materials: {
    materialId: string;
    materialName: string;
    unit: string;
    totalQuantity: number;      // 总量
    dailyQuantity: number;     // 日均用量
    peakDay?: string;          // 峰值日期
    peakQuantity?: number;     // 峰值用量
    currentStock: number;       // 当前库存
    shortageAlert: boolean;    // 是否缺货预警
  }[];

  // 工具需求
  tools: {
    toolId: string;
    toolName: string;
    unit: string;
    requiredCount: number;
    availableCount: number;
    maintenanceAlert: boolean;  // 是否需要保养
  }[];

  // 人员需求
  personnel: {
    role: string;
    requiredCount: number;
    availableCount: number;
    shortageAlert: boolean;
  }[];
}

// 预测计算逻辑
function calculateResourceForecast(
  tasks: PlannedTask[],
  historicalData: TaskExecutionRecord[],
  currentStock: MaterialStock[]
): ResourceForecast {
  // 1. 汇总任务所需物资（根据任务类型和面积）
  // 2. 结合历史消耗数据修正
  // 3. 对比当前库存，计算缺口
  // 4. 考虑峰值日期，合理安排采购
}
```

**采购建议生成**：

```
┌─────────────────────────────────────────────────────────────┐
│ 🛒 采购建议清单                                    生成时间 │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│ ⚠️ 紧急采购（库存不足）                                       │
│ ┌────────────────────────────────────────────────────────┐ │
│ │ 尿素：当前库存80kg，本月需求200kg，缺口120kg           │ │
│ │ 建议：立即采购200kg，预计5月3日前到货                  │ │
│ │ [生成采购单]                                          │ │
│ └────────────────────────────────────────────────────────┘ │
│                                                              │
│ 📋 常规采购（周期性补给）                                     │
│ ┌────────────────────────────────────────────────────────┐ │
│ │ 复合肥：本月需求450kg，建议分3次采购                    │ │
│ │ · 第1批：5月5日  150kg                                │ │
│ │ · 第2批：5月15日 150kg                                │ │
│ │ · 第3批：5月25日 150kg                                │ │
│ └────────────────────────────────────────────────────────┘ │
│                                                              │
│ 🔧 工具保养提醒                                              │
│ ┌────────────────────────────────────────────────────────┐ │
│ │ 背负式喷雾器SPR-001：累计使用320小时，需保养          │ │
│ │ 建议：5月10日前完成保养                               │ │
│ └────────────────────────────────────────────────────────┘ │
│                                                              │
│ [✅ 确认采购计划] [📥 导出清单] [⚙️ 调整阈值]            │
└─────────────────────────────────────────────────────────────┘
```

### 11.6 提醒通知机制

**通知渠道**：

| 渠道 | 触发条件 | 内容 |
|------|---------|------|
| **系统弹窗** | 早上7点任务规划生成 | 今日任务概览，点击查看详情 |
| **短信/微信** | 紧急任务派发 | 任务详情和截止时间 |
| **邮件** | 每日工单汇总报告 | 任务完成率、异常情况 |
| **APP推送** | 巡查问题新发现 | 问题描述和位置 |

**提醒时间表**：

```
┌─────────────────────────────────────────────────────────────┐
│ ⏰ 智能提醒时间表                                            │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│ 06:30  🌅 今日任务规划已生成，点击查看详情                   │
│ 07:00  📋 您有3项待派发任务，请及时处理                      │
│ 08:00  🔔 员工王建国已接受A区灌溉任务                        │
│ 10:00  ⚠️ 临时任务提醒：张三报告B区设备异常                  │
│ 14:00  📊 午间进度：今日任务完成率58%，正常进度              │
│ 17:00  🔔 员工李四完成任务：番茄灌溉（用时1.5小时）          │
│ 18:00  📋 每日汇总：A区任务全部完成，B区1项待处理            │
│ 20:00  🌙 明日任务预览已生成，请提前安排                     │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 十二、核心优势总结

- ✅ **灵活性**：三种模式覆盖不同场景和管理者偏好
- ✅ **渐进式**：支持从"纯人工" → "AI辅助" → "AI全自动"的平滑过渡
- ✅ **可控性**：保留人工最终决策权，AI始终是辅助角色
- ✅ **可扩展**：支持按任务类型、置信度、时间段自动切换模式
- ✅ **统一接口**：`useUnifiedTaskCreation` hook统一三种任务系统的创建流程
- ✅ **可复用组件**：`AIRecommendationPanel`组件可在三个任务系统中复用
- ✅ **智能监测**：自动分析任务完成情况，识别未完成/提前/推迟任务
- ✅ **自动规划**：每日7点前自动生成任务规划，减少管理人员工作负担
- ✅ **月度预览**：根据生产计划和生长周期生成30天任务规划
- ✅ **资源预测**：提前预估物资、工具、人员需求，支持采购计划
- ✅ **状态机完整**：draft→pending_ai→recommended→pending→accepted→in_progress→completed
- ✅ **优化建议机制**：对已选执行人的任务，AI检测更优人选并弹窗建议
- ✅ **全维度因素**：天气、IoT传感器、作物生长周期、人员技能/负荷/位置/表现
- ✅ **动态权重调整**：根据任务类型、紧急程度、面积动态调整匹配权重
- ✅ **33天详细计划**：分7个阶段的完整实施路线图
