# 临时任务业务流程实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 将临时任务纳入统一任务管理体系，复用生产任务的完整流程（创建→发布→接受→执行→提交进度→验收→完成）

**架构：**
- 临时任务复用 Task 数据结构和 useTasks hook
- `sourceType = 'tempTask'` 区分任务来源
- 农事管理临时任务页面：负责创建和管理（负责人操作）
- 我的任务页面：新增"临时任务"Tab（执行人操作）

**技术栈：** React + TypeScript + localStorage + useTasks hook

---

## 文件清单

| 文件 | 操作 | 职责 |
|------|------|------|
| `src/components/labor/myTasks/MyTasksPage.tsx` | 修改 | 新增"临时任务"Tab，筛选 `sourceType === 'tempTask'` |
| `src/components/labor/tempTask/TempTaskPage.tsx` | 修改 | 增强创建流程，同步到 useTasks，使用完整状态机 |
| `src/types/task.ts` | 检查 | 确保 Task.sourceType 支持 'tempTask' |
| `src/hooks/useTasks.ts` | 检查 | 确保 createTask/publishTask 支持 sourceType='tempTask' |

---

## 任务 1：修改 MyTasksPage - 新增"临时任务"Tab

**文件：** `src/components/labor/myTasks/MyTasksPage.tsx`

### 步骤

- [ ] **1. 添加第四个 Tab 按钮**

在现有的三个 Tab（全部任务、问题处理、生产任务）后面添加：

```tsx
<button
  onClick={() => { setTaskFilter('temp'); setCurrentPage(1); }}
  className={`px-6 py-3 text-sm font-medium flex items-center gap-2 border-b-2 transition-colors ${
    taskFilter === 'temp'
      ? 'border-orange-500 text-orange-600 bg-orange-50'
      : 'border-transparent text-gray-500 hover:text-gray-700 hover:bg-gray-50'
  }`}
>
  临时任务
  <span className="px-2 py-0.5 bg-orange-200 text-orange-600 rounded-full text-xs">
    {taskCounts.temp}
  </span>
</button>
```

- [ ] **2. 添加筛选逻辑**

修改 `taskFilter` 类型和筛选逻辑：

```tsx
// 筛选状态类型修改
const [taskFilter, setTaskFilter] = useState<'all' | 'problem' | 'production' | 'temp'>('all');

// 统计临时任务数量
const taskCounts = useMemo(() => ({
  all: myTasks.length,
  problem: myTasks.filter(t => t.sourceProblemId !== undefined).length,
  production: myTasks.filter(t => !t.sourceProblemId && (t as any).sourceType !== 'tempTask').length,
  temp: myTasks.filter(t => (t as any).sourceType === 'tempTask').length,
}), [myTasks]);

// 筛选逻辑添加
const filteredTasks = useMemo(() => {
  switch (taskFilter) {
    case 'problem':
      return myTasks.filter(task => task.sourceProblemId !== undefined);
    case 'production':
      return myTasks.filter(task => !task.sourceProblemId && (task as any).sourceType !== 'tempTask');
    case 'temp':
      // 临时任务 Tab：筛选 sourceType === 'tempTask'
      return myTasks.filter(task => (task as any).sourceType === 'tempTask');
    default:
      return myTasks;
  }
}, [myTasks, taskFilter]);
```

- [ ] **3. 临时任务 Tab 的操作按钮逻辑**

临时任务的执行人操作与生产任务相同，但需要额外处理：

```tsx
// 临时任务 Tab 的操作按钮（复用生产任务逻辑）
{/* 在状态判断后添加临时任务支持 */}
{(task.status === 'pending') && (
  <div className="flex items-center gap-1">
    <button
      onClick={() => handleAccept(task)}
      className="flex items-center gap-1 px-2 py-1.5 text-white bg-green-500 hover:bg-green-600 rounded-lg text-xs font-medium transition-colors"
    >
      <CheckCircle className="w-3 h-3" />
      接受
    </button>
    <button
      onClick={() => openRejectModal(task)}
      className="flex items-center gap-1 px-2 py-1.5 text-white bg-red-500 hover:bg-red-600 rounded-lg text-xs font-medium transition-colors"
    >
      <XCircle className="w-3 h-3" />
      拒绝
    </button>
  </div>
)}
{(task.status === 'accepted' || task.status === 'in_progress') && (
  <button
    onClick={() => openFeedbackModal(task)}
    className="flex items-center gap-1.5 px-3 py-1.5 text-white bg-blue-500 hover:bg-blue-600 rounded-lg text-sm font-medium transition-colors"
  >
    <Edit className="w-4 h-4" />
    提交进度
  </button>
)}
{task.status === 'rejected' && (
  <>
    <button
      onClick={() => {
        const unifiedTask = unifiedTasks.find(t => t.taskCode === task.id || t.id === task.id);
        if (unifiedTask) {
          continueExecution(unifiedTask.id);
        }
      }}
      className="flex items-center gap-1.5 px-3 py-1.5 text-white bg-orange-500 hover:bg-orange-600 rounded-lg text-sm font-medium transition-colors"
    >
      <Play className="w-4 h-4" />
      继续执行
    </button>
    <button
      onClick={() => openDetailModal(task)}
      className="flex items-center gap-1.5 px-3 py-1.5 text-gray-600 hover:text-white bg-gray-100 hover:bg-gray-500 rounded-lg text-sm font-medium transition-colors"
    >
      <Eye className="w-4 h-4" />
      查看
    </button>
  </>
)}
{(task.status === 'waiting_acceptance' || task.status === 'completed') && (
  <button
    onClick={() => openDetailModal(task)}
    className="flex items-center gap-1.5 px-3 py-1.5 text-gray-600 hover:text-white bg-gray-100 hover:bg-gray-500 rounded-lg text-sm font-medium transition-colors"
  >
    <Eye className="w-4 h-4" />
    查看
  </button>
)}
```

---

## 任务 2：修改 TempTaskPage - 增强创建流程

**文件：** `src/components/labor/tempTask/TempTaskPage.tsx`

### 步骤

- [ ] **1. 修改 onSubmit 回调，使用完整的 useTasks 方法**

找到 `useTempTaskForm` 的 `onSubmit` 回调，修改创建逻辑：

```tsx
const {
  createTask,     // 新增：从 useTasks 获取
  publishTask,    // 新增：用于自动发布
} = useTasks();

// onSubmit 修改为创建并自动发布
onSubmit: (taskData, status) => {
  if (editingTask) {
    // 更新逻辑（后续实现）
    console.log('更新临时任务:', editingTask.id, taskData);
  } else {
    // ========== 数据闭环：新建临时任务 ==========
    // 使用 useTasks 创建任务（sourceType = 'tempTask'）
    const newTask = createTask({
      title: taskData.title || '',
      type: taskData.tempTaskType || 'other',
      typeName: taskData.tempTaskType || '其他',
      status: 'draft',
      priority: mapUrgencyToPriority(taskData.urgency),
      progress: 0,
      sourceType: 'tempTask',
      assigneeId: taskData.assigneeId || '',
      assigneeName: taskData.assigneeName || '待分配',
      assignerId: 'admin',
      assignerName: '管理员',
      greenhouseName: taskData.workLocation || '',
      cropName: '',
      feedbackRequirements: [],
      dueDate: taskData.dueDate,
      estimatedHours: taskData.estimatedHours || 1,
    }, 'tempTask');

    // 自动发布任务（status: draft → pending）
    if (status === 'pending') {
      publishTask(newTask.id);
    }

    // ========== 数据闭环：同步到农事操作记录 ==========
    addTempTaskRecord({
      operationType: taskData.tempTaskType || '其他',
      operationTypeName: `临时任务-${taskData.tempTaskType || '其他'}`,
      status: status === 'pending' ? 'pending' : 'draft',
      greenhouseId: '',
      greenhouseName: taskData.workLocation || '',
      cropName: '',
      operatorId: taskData.assigneeId || '',
      operatorName: taskData.assigneeName || '待分配',
      operationDate: new Date().toISOString().split('T')[0],
      sourceId: newTask.id,
      sourceCode: newTask.taskCode,
      progress: 0,
      remarks: status === 'pending' ? '临时任务已发布' : '临时任务已创建（草稿）',
    });
  }
  closeFormModal();
}
```

其中 `mapUrgencyToPriority` 函数：

```tsx
// 紧急程度映射到优先级
const mapUrgencyToPriority = (urgency?: string): 'urgent' | 'high' | 'normal' => {
  switch (urgency) {
    case 'critical': return 'urgent';
    case 'urgent': return 'high';
    default: return 'normal';
  }
};
```

- [ ] **2. 修改详情弹窗操作按钮**

临时任务的负责人操作（验收/驳回）复用 TaskDispatchPage 的逻辑：

```tsx
// 验收通过
const handleAcceptComplete = (task: Task) => {
  acceptCompletion(task.id);
  // 同步到农事操作记录
  addTempTaskRecord({...});
  closeDetailModal();
  window.location.reload();
};

// 审核驳回
const handleRejectComplete = (task: Task, reason: string) => {
  rejectForRework(task.id, reason);
  // 同步到农事操作记录
  addTempTaskRecord({...});
  closeDetailModal();
  window.location.reload();
};
```

- [ ] **3. 修改批量编辑逻辑**

确保批量编辑也同步到 useTasks：

```tsx
const handleBatchEditConfirm = (editedTasks: Record<string, Partial<TempTask>>) => {
  // 批量编辑功能暂未接入 useTasks，可后续扩展
  console.log('批量编辑暂未接入数据闭环:', editedTasks);
  setSelectedRows([]);
  setBatchEditMode(false);
};
```

---

## 任务 3：验证数据闭环

### 步骤

- [ ] **1. 创建临时任务后验证**

在 TempTaskPage 创建临时任务：
1. 检查 localStorage `yuanxingtu_tasks` 中是否有 `sourceType='tempTask'` 的任务
2. 检查 MyTasksPage 临时任务 Tab 是否显示该任务

- [ ] **2. 执行人接受任务后验证**

在 MyTasksPage 临时任务 Tab：
1. 点击"接受"按钮
2. 检查任务状态是否变为 `accepted`
3. 检查 useTasks 的操作记录是否创建

- [ ] **3. 执行人提交进度后验证**

1. 点击"提交进度"按钮
2. 填写反馈信息（工作量、照片等）
3. 检查任务状态是否变为 `waiting_acceptance`
4. 检查 TaskRecord 是否包含完整反馈数据

- [ ] **4. 负责人验收后验证**

在 TempTaskPage：
1. 找到待验收的临时任务
2. 点击"验收通过"
3. 检查任务状态是否变为 `completed`
4. 检查 MyTasksPage 临时任务 Tab 是否同步更新

---

## 任务 4：构建验证

### 步骤

- [ ] **1. 运行构建命令**

```bash
npm run build
```

- [ ] **2. 检查是否有编译错误**

如有错误，根据错误信息修复：
- 类型错误：检查字段映射是否正确
- 导入错误：检查模块路径是否正确

- [ ] **3. 验证功能完整性**

确认以下功能正常：
- [ ] 临时任务创建成功
- [ ] 临时任务出现在 MyTasksPage 临时任务 Tab
- [ ] 执行人可接受/拒绝任务
- [ ] 执行人可提交进度
- [ ] 负责人可验收/驳回
- [ ] 操作记录完整显示

---

## 注意事项

1. **字段映射**：
   - `tempTaskType` → `type`
   - `urgency` → `priority` (critical→urgent, urgent→high, normal→normal)
   - `workLocation` → `greenhouseName`

2. **状态同步**：
   - TempTaskPage 和 MyTasksPage 操作的是同一个 Task 对象
   - 使用 `sourceId` 或 `sourceCode` 关联两个系统的数据

3. **feedbackRequirements**：
   - 临时任务的反馈必填项与生产任务相同
   - 可在创建时通过配置设置

---

## 验收标准

1. 临时任务创建后自动出现在 MyTasksPage 的"临时任务"Tab
2. 执行人可完成完整流程：接受 → 执行 → 提交进度 → 待验收
3. 负责人可完成验收流程：验收通过 / 驳回
4. 操作记录完整保存，包括反馈数据
5. `npm run build` 构建成功