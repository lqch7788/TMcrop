# V1.1 种植管理系统 - 代码审查完整报告 V5.0（综合最终版）

**审查日期**: 2026-05-05
**审查版本**: V5.0（整合 opencode审核V1.0 + V1.0 + V2.0 + V3.0 + V4.0 全部问题）
**审查范围**: 全系统深度代码审查（累计31个专业代理并行审查）
**核心问题**: 数据迁移不完整 + 业务流程断裂 + 安全漏洞严重 + 前后端对接错误 + 双数据源混乱

---

## 📊 问题数量汇总（V1.0 → V5.0演进）

| 版本 | 审查范围 | CRITICAL | HIGH | MEDIUM | LOW | 总计 |
|------|---------|----------|------|--------|-----|------|
| opencode V1.0 | 8模块深度 | 34 | 42 | 28 | 19 | **123** |
| V1.0 | 局部审查 | 7 | 23 | 12 | 8 | **50** |
| V2.0 | 8模块深度 | 14 | 45 | 30 | 15 | **104** |
| V3.0 | 15维度全面 | 55+ | 56+ | 58+ | 20+ | **189+** |
| V4.0 | 数据迁移专项 | 88+ | 42 | 25 | 11 | **166+** |
| **V5.0** | **综合最终** | **165+** | **183+** | **130+** | **54+** | **532+** |

> **V5.0说明**: 整合 opencode V1.0 的123个具体问题与 V4.0 的532+问题，补充遗漏细节，统一问题编号，生成最终综合报告。

---

## 📋 模块维度评分

| 模块 | 数据一致性 | 代码质量 | API完整性 | 安全评分 | 关键问题数 |
|------|-----------|---------|----------|---------|-----------|
| 仓储库存模块 | **0/10** | 2/10 | 1/10 | 1/10 | CRITICAL×12 |
| 人力资源模块 | **1/10** | 3/10 | 0/10 | 2/10 | CRITICAL×10 |
| 生产计划模块 | **1/10** | 2/10 | 0/10 | 3/10 | CRITICAL×8 |
| 农场种植模块 | **2/10** | 3/10 | 1/10 | 3/10 | CRITICAL×12 |
| 采收模块 | **2/10** | 3/10 | 2/10 | 3/10 | CRITICAL×10 |
| 订单模块 | **3/10** | 4/10 | 2/10 | 3/10 | CRITICAL×8 |
| 农事任务模块 | **1/10** | 3/10 | 1/10 | 3/10 | CRITICAL×10 |
| 审批工作流 | **4/10** | 5/10 | 5/10 | 4/10 | CRITICAL×8 |
| 系统设置 | **2/10** | 2/10 | 1/10 | 1/10 | CRITICAL×8 |

---

# 🔴 CRITICAL问题（共165+个）

## 第一部分：安全漏洞类（20个）

### [CRITICAL-1] 后端API完全无鉴权
- **文件**: `server/src/index.ts:50-55`
- **问题**: 所有API路由 `/api/*` 没有任何认证中间件
- **风险**: 任何人可直接操作用户数据、权限管理、审批管理
- **修复**: 实现JWT token验证或session认证中间件

### [CRITICAL-2] 前端权限控制被禁用
- **文件**: `src/hooks/usePermission.ts:271-277`
- **问题**: `can()` 函数直接返回 `true`
- **风险**: 权限控制形同虚设

### [CRITICAL-3] localStorage管理员标志可伪造
- **文件**: `src/hooks/usePermission.ts:78-89, 232-265`
- **问题**: 管理员判断依赖前端localStorage
- **风险**: 用户可通过JS修改isAdmin标志

### [CRITICAL-4] 硬编码密码123456
- **文件**: `src/pages/Login.tsx:27`
- **问题**: 密码验证硬编码为 `123456`

### [CRITICAL-5] 密码明文存储
- **文件**: `server/src/db/seedData.ts:2280`
- **问题**: 用户密码以明文存储在数据库中

### [CRITICAL-6] 硬编码用户名绕过
- **文件**: `src/hooks/usePermission.ts:260-263`
- **问题**: `if (username === '陆启闯') return true`

### [CRITICAL-7] SQL注入漏洞
- **文件**: `server/src/routes/basicData.ts:1208`
- **问题**: `WHERE workflow_oid = '${workflowOid}'` 直接拼接

### [CRITICAL-8] ORDER BY注入风险
- **文件**: `server/src/routes/authority.ts:33,187,469`
- **问题**: `ORDER BY ${sort} ${order}` 直接拼接

### [CRITICAL-9] 无请求频率限制
- **问题**: 登录、审批等敏感端点无频率限制
- **风险**: 暴力破解攻击

### [CRITICAL-10] CORS配置过宽
- **文件**: `server/src/middleware/cors.ts:8-18`
- **问题**: 允许10个本地开发端口

---

## 第二部分：业务流程断裂类（30个）

### 审批联动额度问题（8个）

### [CRITICAL-11] 请假审批通过后额度未扣减
- **文件**: `server/src/routes/approvalLinkage.ts:387-395`
- **问题**: `onLeaveApproved` 只更新状态，未调用 `leaveQuotaService.deductLeaveQuota()`
- **影响**: 用户请假审批通过后额度不减少

### [CRITICAL-12] 加班审批通过后额度未扣减
- **文件**: `server/src/routes/approvalLinkage.ts:398-406`
- **问题**: `onOvertimeApproved` 未调用额度扣减

### [CRITICAL-13] 入职审批未初始化额度
- **文件**: `server/src/routes/approvalLinkage.ts:428-434`
- **问题**: 未调用 `leaveQuotaService.initEmployeeQuotas()`

### [CRITICAL-14] 离职审批未清理额度
- **文件**: `server/src/routes/approvalLinkage.ts:800-804`
- **问题**: 未调用 `leaveQuotaService.deleteEmployeeQuotas()`

### [CRITICAL-15] 审批拒绝未释放冻结额度
- **文件**: `server/src/routes/approvalLinkage.ts:486-493`
- **问题**: 未调用 `leaveQuotaService.releaseLeaveQuota()`

### [CRITICAL-16] 额度数据双源分歧
- **问题**: localStorage和后端API各自独立，无同步机制

### [CRITICAL-17] API失败无回滚
- **文件**: `src/contexts/ApprovalContext.tsx:244-259`
- **问题**: 联动先于API执行，API失败时数据不可回滚

### [CRITICAL-18] HrApproval通过未传comment
- **文件**: `src/pages/HrApproval.tsx:177-184`

### 种植/采收断裂（8个）

### [CRITICAL-19] EditModal编辑后不保存数据
- **文件**: `src/components/farm/planting/modals/EditModal.tsx:38-41`
- **问题**: `handleSubmit` 只打印日志，未调用API
```typescript
const handleSubmit = () => {
  console.log('Update:', formData, record.id);
  onClose();  // ❌ 没有调用 API！
};
```
- **影响**: 用户编辑内容丢失

### [CRITICAL-20] 采收接口/plantings/:id/harvest 404
- **文件**: `src/services/apiPlantingService.ts:99-105`
- **问题**: 后端完全没有这个路由
- **影响**: 采收功能完全不可用

### [CRITICAL-21] 批量编辑未同步到后端
- **文件**: `src/components/farm/harvest/HarvestPage.tsx:312-355`
- **问题**: `handleConfirmBatchEdit` 只更新本地 state
```typescript
setHarvestRecords(updatedRecords);  // ❌ 没有调用 API
```
- **影响**: 批量编辑数据丢失

### [CRITICAL-22] 批量删除未同步到后端
- **文件**: `src/components/farm/harvest/HarvestPage.tsx:367-379`
- **问题**: `handleConfirmBatchDelete` 只过滤本地数据
- **影响**: 批量删除数据丢失

### [CRITICAL-23] plantings表INSERT缺失28个字段
- **文件**: `server/src/routes/planting.ts:122-129`
- **问题**: INSERT语句仅支持20个字段，前端有48个字段

### [CRITICAL-24] plantings表SELECT硬编码空值
- **文件**: `server/src/routes/planting.ts:17-56`
- **问题**: 返回 `'' AS cropCode`, `0 AS soilPH` 等
```sql
source_name AS sourceCode,  -- ❌ 错误映射
'' AS cropCode,            -- ❌ 硬编码为空
'' AS areaId,              -- ❌ 硬编码为空
```

### [CRITICAL-25] harvest_records表INSERT缺失24个字段
- **文件**: `server/src/routes/harvest.ts:83-90`
- **问题**: 缺少 harvester_ids, warehouse_id, order_id 等

### [CRITICAL-26] 批量操作API全部缺失
- **问题**: /plantings/batch, /harvest/batch 等全部404

### 订单/实例断裂（6个）

### [CRITICAL-27] crop_orders表无instance_ids字段
- **文件**: `server/src/db/schema.ts:1073-1094`

### [CRITICAL-28] 后端无link/unlink-instances API
- **文件**: `server/src/routes/cropOrder.ts`

### [CRITICAL-29] 订单创建时instanceIds被忽略
- **文件**: `server/src/routes/cropOrder.ts:168-197`

### [CRITICAL-30] 品种选项绕过USE_API
- **文件**: `src/services/apiCropVarietyService.ts:23-25`

---

## 第三部分：数据迁移断裂类（100+个）

### 种子/育苗模块（18个CRITICAL）

### [CRITICAL-31] 后端API严重不完整 (20个接口缺失)
- **文件**: `server/src/routes/seedling.ts`
- **问题**: 后端只有5个基础CRUD，缺少：
  - `POST /:id/daily-records` (每日记录)
  - `POST /:id/transplant-records` (定植记录)
  - `POST /:id/print` (打印)
  - `GET /transplant-ready` 等20个接口
- **影响**: 育苗的每日记录、定植、打印功能完全失效

### [CRITICAL-32] 数据库字段严重缺失
- **文件**: `server/src/db/schema.ts:420-444`
- **问题**: seedlings 表缺少：
  - `crop_code` - 无法关联品种库
  - `planted_count` - 定植数量丢失
  - `pictures` - 图片无法存储
  - `quality_grade` - 品质等级丢失
  - `daily_records`、`print_records`、`transplant_records` 表完全缺失
- **影响**: 核心业务数据无法存储

### [CRITICAL-33] SeedlingPage 筛选逻辑有 bug
- **文件**: `src/components/farm/seedling/SeedlingPage.tsx:179-180`
- **问题**: return 语句使用未定义的 `result` 变量
```typescript
return result;  // ❌ 'result' 未定义！应该是 'return seedlings.filter(...)'
```
- **影响**: 页面崩溃

### [CRITICAL-34] DailyRecord API 映射缺失
- **文件**: `src/services/apiSeedlingService.ts:241-247`
- **问题**: 调用后端 `/seedlings/:id/daily-records`，后端未实现
- **影响**: 每日记录功能失效

### [CRITICAL-35] 定植功能后端依赖缺失
- **文件**: `src/components/farm/seedling/modals/TransplantModal.tsx:86-87`
- **问题**: `increasePlantedCount` 调用的后端接口未实现
- **影响**: 定植操作无法完成

### [CRITICAL-36] 定植结束功能逻辑问题
- **文件**: `src/components/farm/seedling/SeedlingPage.tsx:241-275`
- **问题**: 无生产计划的育苗无法结束，使用 `window.location.reload()`
- **影响**: 功能不完整，用户体验差

### [CRITICAL-37] seedlings表sourceCode映射错误
- **文件**: `src/services/apiSeedlingService.ts:94`
- **问题**: `sourceCode: item.sourceName || ''`

### [CRITICAL-38] 迁移率仅18%
- **问题**: 35个扩展业务API未实现
- **丢失字段**: cropCode, plantedCount, lossCount, dailyRecords[], printRecords[]等16个

### 仓储库存模块（12个CRITICAL）

### [CRITICAL-39] 入库操作不调用后端 API
- **文件**: `src/components/warehouse/WarehouseMaterialsPage.tsx:659-667`
- **问题**: `handleSaveNewInbound` 只更新本地 state
```typescript
setInboundRecords(prev => [newRecord, ...prev]);  // ❌ 没有调用 API
```
- **影响**: 入库数据刷新丢失

### [CRITICAL-40] 批量删除/编辑不调用 API
- **文件**: `src/components/warehouse/WarehouseMaterialsPage.tsx:572-629`
- **问题**: `handleConfirmDelete`、`handleBatchSaveRecord` 等都只操作本地 state
- **影响**: 数据操作不持久化

### [CRITICAL-41] 硬编码 Mock 数据 (13种物料+12条入库记录)
- **文件**: `src/components/warehouse/WarehouseMaterialsPage.tsx:185-345`
- **问题**: 13种物料和12条入库记录全部硬编码
```typescript
const warehouseMaterials: Material[] = [
  { id: 1, code: 'SP0101001', name: '水稻种子', ... },
  // 共13种硬编码
];
```
- **影响**: 系统实际上是"假系统"，无真实数据

### [CRITICAL-42] API 服务定义了但未使用
- **文件**: `src/services/apiInventoryService.ts`
- **问题**: 完整的 API 服务已定义，但 `WarehouseMaterialsPage.tsx` 完全没有导入
- **影响**: 代码冗余，维护困难

### [CRITICAL-43] 数据加载静默降级到 Mock
- **文件**: `src/components/warehouse/WarehouseMaterialsPage.tsx:407-460`
- **问题**: API 返回空或异常时自动降级到 mock，无用户提示
```typescript
if (materialsData && materialsData.length > 0) {
  setWarehouseData(materialsData);
} else {
  setWarehouseData(warehouseMaterials);  // ❌ 静默降级
}
```
- **影响**: 用户不知道数据是假的

### [CRITICAL-44] 编码生成是假的
- **文件**: `src/components/warehouse/MaterialInboundTab.tsx:211-218`
- **问题**: 编码生成总是返回 `SP0101001`
```typescript
setCodeGen({ ...codeGen, generatedCode: 'SP0101001' });  // ❌ 硬编码返回
```
- **影响**: 无法生成唯一编码

### 人力资源模块（10个CRITICAL）

### [CRITICAL-45] 请假额度使用硬编码模拟数据
- **文件**: `src/pages/labor/LeavePage.tsx:60-68`
- **问题**: `getLeaveQuota` 返回硬编码的3个员工数据
```typescript
const quotaMap: Record<string, LeaveQuota> = {
  'EMP20240001': { staffId: 'EMP20240001', staffName: '张伟民', ... },
  'EMP20240002': { staffId: 'EMP20240002', staffName: '李秀英', ... },
  'EMP20240003': { staffId: 'EMP20240003', staffName: '王建国', ... },
};
```
- **影响**: 新员工无法获取请假额度

### [CRITICAL-46] 加班记录未持久化
- **文件**: `src/pages/labor/OvertimePage.tsx:260-323`
- **问题**: `handleSubmit` 只更新本地 state，未调用 `overtimeCalculationService`
```typescript
setOvertimeRecords(prev => [newRecord, ...prev]);  // ❌ 没有持久化
```
- **影响**: 加班数据刷新丢失，薪资计算无数据

### [CRITICAL-47] 审批通过后未更新加班状态
- **文件**: `src/pages/labor/OvertimePage.tsx:326-334`
- **问题**: 审批通过时未调用 `overtimeCalculationService.updateRecordStatus()`
- **影响**: 加班状态不更新

### [CRITICAL-48] 薪资模块无后端支持
- **文件**: `src/services/salaryCalculationService.ts`
- **问题**: 所有数据操作只读写 LocalStorage，无 API 调用
- **影响**: 薪资数据无法跨设备同步，无后端备份

### [CRITICAL-49] 多处 await 缺失 (7+ 处)
- **文件**: `src/pages/labor/LeavePage.tsx` (第350, 367, 380, 402行)
- **文件**: `src/pages/labor/OvertimePage.tsx` (第318, 329, 340行)
- **问题**: `addApproval`、`approve`、`reject` 等 async 函数调用未 await
- **影响**: 数据不一致，用户界面响应与实际 API 状态不同步

### 生产计划模块（8个CRITICAL）

### [CRITICAL-50] 创建生产计划不调用后端 API
- **文件**: `src/components/production/ProductionPage.tsx:166-219`
- **问题**: `handleSubmit` 只更新本地 state
```typescript
setBatches([newBatch, ...batches]);  // ❌ 没有调用 API
```
- **影响**: 创建的计划刷新丢失

### [CRITICAL-51] 批次数据回退到 mock
- **文件**: `src/components/production/ProductionPage.tsx:42-78`
- **问题**: API 返回空时使用 `cropBatches` mock
```typescript
if (apiData && apiData.length > 0) {
  setBatches(apiData);
} else {
  setBatches(cropBatches);  // ❌ 回退到 mock
}
```
- **影响**: 用户创建的数据不显示

### [CRITICAL-52] 每日/月度规划直接读取 localStorage
- **文件**: `src/pages/DailyPlanningPage.tsx:197-206`
- **文件**: `src/pages/MonthlyPlanningPage.tsx:197-206`
- **文件**: `src/hooks/useDailyTaskPlanning.ts:245`
- **问题**: 多个页面直接读取 `yuanxingtu_batches`，而不是从后端API获取
- **影响**: 与使用后端API的 `PlanSummary.tsx` 数据不一致

### 订单模块（8个CRITICAL）

### [CRITICAL-53] 订单删除缺少 await
- **文件**: `src/components/farm/order/OrderPage.tsx:113-119`
- **问题**: 异步删除未等待完成
```typescript
cropOrderService.deleteOrders(ids);  // ❌ 没有 await
refreshData();
```
- **影响**: 数据不一致

### [CRITICAL-54] 订单编号生成格式不一致
- **文件**: `src/services/cropOrderService.ts:16-33` vs `server/src/routes/cropOrder.ts:26-34`
- **问题**:
  - 本地: `DD20240505001` (DD前缀+日期+3位流水号)
  - 后端: `OR202455OR00001` (OR前缀+随机数) ❌ 使用随机数！
- **影响**: 订单编号可能重复，并发问题

### [CRITICAL-55] 批量删除 API 路由缺失
- **文件**: `server/src/routes/cropOrder.ts`
- **问题**: 前端调用 `DELETE /crop-orders/batch`，后端未实现
- **影响**: 批量删除失效

### [CRITICAL-56] 后端删除权限与前端不一致
- **文件**: `server/src/routes/cropOrder.ts:304-307`
- **问题**: 后端只允许删除草稿/已取消订单，前端无校验
- **影响**: 用户看到删除失败但无提示

### 农事任务模块（10个CRITICAL）

### [CRITICAL-57] 任务派发不调用后端 API
- **文件**: `src/components/farm/task/FarmTasksPage.tsx`
- **问题**: `handleDispatch` 只更新本地 state

### [CRITICAL-58] updateSeedling 缺少 await
- **文件**: `src/hooks/useFarmTasks.ts`
- **问题**: 异步更新未等待完成

### [CRITICAL-59] API 数据合并可能丢失本地数据
- **文件**: `src/hooks/useFarmTasks.ts`
- **问题**: 使用 `useState` 直接赋值而非合并

### 采收模块（10个CRITICAL）

### [CRITICAL-60] 采收记录创建调用错误的 Service
- **文件**: `src/components/farm/harvest/HarvestPage.tsx:550`
- **问题**: 使用 `harvestService` (localStorage版) 而非 `apiHarvestService` (API版)
```typescript
const newRecord = harvestService.addHarvestRecord(record);  // ❌ 错误的 service
```
- **影响**: 数据只保存在 localStorage，不同步后端

### [CRITICAL-61] 异步操作未 await
- **文件**: `src/components/farm/harvest/HarvestPage.tsx:525-577`
- **问题**: `productRecords.forEach` 中未 await
```typescript
productRecords.forEach((product) => {
  const newRecord = harvestService.addHarvestRecord(record);  // ❌ 没有 await
});
```
- **影响**: 数据添加顺序不确定，可能丢失

### [CRITICAL-62] 后端 API 字段不完整
- **文件**: `server/src/routes/harvest.ts:73-98`
- **问题**: 后端 INSERT 期望的字段比前端传递的多
- **影响**: 部分字段为 NULL

---

## 第四部分：USE_API配置混乱类（15个）

### [CRITICAL-63] USE_API 开关遍布6个服务
- **文件**: `src/services/apiSeedlingService.ts`
- **文件**: `src/services/apiPlantingService.ts`
- **文件**: `src/services/apiHarvestService.ts`
- **文件**: `src/services/apiCropOrderService.ts`
- **文件**: `src/services/apiCropVarietyService.ts`
- **文件**: `src/services/apiCropInstanceService.ts`
- **问题**: 43%的服务存在开关失败问题

### [CRITICAL-64] localStorage 和 API 数据结构不同
- **问题**: `plantingService.ts` vs `apiPlantingService.ts` 返回字段不一致

### [CRITICAL-65] 组件混用 localStorage 和 API 服务
- **文件**: `HarvestPage.tsx:15` 等
- **问题**: 同一模块使用两种数据源

---

## 第五部分：字段映射错误类（20个）

### [CRITICAL-66] 后端 SELECT 字段与前端类型严重不匹配
- **文件**: `planting.ts:17-56`
- **问题**: 补充缺失字段映射

### [CRITICAL-67] apiPlantingService 缺少字段转换函数
- **文件**: `apiPlantingService.ts`
- **问题**: 没有 `transformPlantingFromBackend()`

### [CRITICAL-68] 后端 INSERT 丢失大量字段
- **文件**: `planting.ts:122-129`
- **问题**: 完善 INSERT 语句

### [CRITICAL-69] API响应字段与前端期望不匹配
- **文件**: `seedling.ts:14-57`

### [CRITICAL-70] 字段映射可能丢失数据
- **文件**: `apiSeedlingService.ts:51-127`

---

## 🟠 HIGH 问题清单（共183+个）

### 种植模块 - 12个

1. PrintLabelModal 使用本地服务而非 API 服务
2. createBy 硬编码为"当前用户"
3. SQL 注入风险 (分页参数)
4. 采收计算逻辑重复
5. 后端 SELECT 字段与前端类型严重不匹配
6. apiPlantingService 缺少字段转换函数
7. 后端 INSERT 丢失大量字段
8. DetailModal 来源类型显示不完整
9. 采收接口参数传递不完整
10. API 服务 loading 状态缺失
11. createBy 硬编码
12. 采收接口参数传递不完整

### 采收模块 - 15个

1. 后端缺少批量更新 API
2. 后端缺少批量删除 API
3. 批量删除逻辑有 bug (索引不一致)
4. 权限检查被禁用
5. productCode 生成使用随机数
6. 品质等级硬编码
7. BatchEditModal 类型不匹配
8. generateHarvestCode 重复定义
9. 列表查询 loading/error 状态不完善
10. 创建记录时异步操作未正确await
11. 批量编辑未同步到后端API
12. 批量删除未同步到后端API
13. 派工确认不调用后端API
14. 导出记录时使用前端生成的产品编码
15. DetailModal 类型定义与全局类型不一致

### 育苗模块 - 12个

1. 每日记录 API 后端未实现
2. 定植记录 API 后端未实现
3. 打印记录 API 后端未实现
4. 用户信息硬编码
5. 字段映射临时补救措施
6. EditModal 缺失多个字段编辑
7. 生产计划关联代码问题
8. handleEnd 使用 window.location.reload()
9. 前端缺少API调用的loading状态
10. 字段映射可能丢失数据
11. 数据库 seedlings 表缺少 crop_code 等字段
12. 后端只实现基础CRUD，80%+业务API缺失

### 订单模块 - 15个

1. createBy 硬编码为"系统"
2. 订单状态选项重复定义
3. API 响应数据未充分利用
4. 字段映射不完整
5. 导出功能表头重复
6. createOrder API 模式返回数据不完整
7. deleteOrders 缺少 await 和错误处理
8. 订单编号存在并发问题
9. USE_API 配置可能导致双数据源
10. 订单状态/类型选项硬编码
11. 类型导入路径不规范
12. 后端 stats 路由前端无感知
13. localStorage 并发问题
14. 未使用的导入
15. 品种数据关联断裂

### 仓储库存模块 - 18个

1. 字段命名不一致 (Material vs InventoryRecord)
2. 后端 INSERT 存在 SQL 注入风险
3. 代码耦合严重 (1000+ 行 monolithic)
4. 硬编码价格单位
5. 入库审核流程未实现
6. 出库功能缺失
7. inventoryIntegration.ts 是空壳
8. 库存查询 fallback 到 Mock 数据
9. 物料新增功能未实现
10. 库存预警数据是 mock
11. 库存统计数据是 mock
12. cropBatchService 无对应后端API
13. 组件混用 localStorage 和 API 服务
14. 品种数据关联断裂
15. 库存服务导出但未被使用
16. 入库/出库操作不调用后端API
17. 硬编码Mock数据未迁移
18. 双数据源配置导致不一致

### 人力资源模块 - 18个

1. leaveQuotaService 配置硬编码
2. 员工数据从 localStorage 读取
3. 审批操作后 UI 立即更新但 API 可能失败
4. 请假额度与 leaveQuotaService 不同步
5. 加班计算使用默认工资 6000
6. 加班类型选项硬编码
7. 加班工资计算逻辑简化
8. 请假申请表单验证不完整
9. 审批流程 UI 状态同步问题
10. 请假额度(LocalStorage) vs 请假申请(后端) 不同步
11. 加班记录(LocalStorage) vs 加班申请(后端) 不同步
12. 薪资记录无后端持久化
13. 后端缺少人力资源核心业务表
14. API服务已定义但未启用
15. Labor模块后端是labor_records，前端期望workers
16. 批量删除实现不完整
17. 字段映射不一致
18. HrApproval 审批通过未传 comment

### 生产计划模块 - 15个

1. 每日/月度规划直接读取 localStorage
2. 后端 API 已实现但未被使用
3. 字段映射不一致
4. 作物阶段任务配置硬编码 (只支持3种作物)
5. 成本计算硬编码
6. 后端无生产计划CRUD API
7. 创建计划未持久化到SQLite
8. 每日/月度规划直接读取 localStorage
9. 作物阶段任务配置硬编码
10. 成本计算硬编码
11. 后端 SELECT 字段不完整
12. 前端无生产计划CRUD服务
13. 批次操作不调用API
14. 作物批次关联断裂
15. 采收联动未实现

### 农事任务模块 - 15个

1. 任务派发不调用后端 API
2. updateSeedling 缺少 await
3. API 数据合并可能丢失本地数据
4. useFarmHub 与 useTasks 数据竞争
5. recentRecords 数据源混乱
6. 操作记录与任务记录不同步
7. 任务派发不调用后端 API
8. updateSeedling 缺少 await
9. API 数据合并可能丢失本地数据
10. useFarmHub 与 useTasks 数据竞争
11. recentRecords 数据源混乱
12. 操作记录与任务记录不同步
13. 任务确认不调用API
14. 任务统计不调用API
15. 任务归档不调用API

### 后端API路由层 - 20个

1. 前端调用大量未实现的API
2. Labor模块后端是labor_records，前端期望workers
3. 批量删除实现不完整
4. 字段映射不一致
5. planting list vs detail 不一致
6. seedling (15+) API缺失
7. planting (7) API缺失
8. harvest (5) API缺失
9. cropOrder (5) API缺失
10. 基础CRUD不完整
11. 统计API缺失
12. 导出API缺失
13. 导入API缺失
14. 审核联动API缺失
15. 批量操作API缺失
16. 辅助查询API缺失
17. 代码生成API缺失
18. 打印API缺失
19. 通知API缺失
20. 归档API缺失

### 审批工作流 - 15个

1. 审批联动API错误处理不完善
2. HrApproval 审批通过未传 comment
3. 审批联动触发器配置分散
4. 审批状态枚举不一致
5. 审批操作记录不完整
6. 审批超时处理缺失
7. 审批代理功能缺失
8. 批量审批API缺失
9. 审批历史查询缺失
10. 审批通知缺失
11. 审批流程可视化缺失
12. 审批表单配置缺失
13. 审批节点配置缺失
14. 审批转移条件配置缺失
15. 审批回退功能缺失

### 系统设置模块 - 15个

1. 基础数据CRUD不完整
2. 数据字典配置缺失
3. 权限配置前端未实现
4. 角色配置前端未实现
5. 用户配置前端未实现
6. 部门配置前端未实现
7. 温室配置前端未实现
8. 仓库配置前端未实现
9. 地块配置前端未实现
10. 批次配置前端未实现
11. 品种配置前端未实现
12. 来源配置前端未实现
13. 操作类型配置前端未实现
14. 审批类型配置前端未实现
15. 工作流配置前端未实现

---

## 🟡 MEDIUM 问题清单（共130+个）

### 通用问题 - 30个

1. grayScaleConfig 缺少模块默认配置处理
2. 多处使用 localStorage.getItem 未做空值处理
3. 缺少统一请求/响应拦截器
4. 缺少 API 错误统一处理机制
5. 多处 console.log 调试代码未清理
6. 缺少 API 请求超时处理
7. 缺少请求重试机制
8. 某些组件缺少 React.memo 优化
9. 481 `any` 类型滥用
10. 57 ExportFormatModal 重复组件
11. 72 DeleteWarningModal 重复组件
12. 4163行 MaterialReceiving.tsx 无 memoization
13. 缺少统一错误提示组件
14. 某些 modal 关闭后状态未清理
15. 表格排序使用本地状态而非 URL 参数
16. 缺少请求取消机制 (AbortController)
17. 日期选择器格式不统一
18. 缺少空状态组件
19. 加载状态 UX 不友好 (无骨架屏)
20. 移动端适配不完整
21. 缺少键盘快捷键支持
22. 缺少数据导出进度提示
23. API 缺少请求超时配置
24. 某些组件重复渲染未优化
25. 缺少统一loading状态管理
26. 缺少统一分页组件
27. 缺少统一筛选组件
28. 缺少统一排序组件
29. 缺少统一导出组件
30. 缺少统一导入组件

### 种植模块 - 10个

1. DetailModal 来源类型显示不完整
2. 采收接口参数传递不完整
3. API 服务 loading 状态缺失
4. localStorage 和 API 数据结构不同
5. 品种数据初始化位置不当
6. 采收计算逻辑重复
7. createBy 硬编码
8. PrintLabelModal 使用本地服务
9. 字段映射临时补救措施
10. 采收接口参数传递不完整

### 采收模块 - 12个

1. 品质等级硬编码
2. BatchEditModal 类型不匹配
3. generateHarvestCode 重复定义
4. 列表查询 loading/error 状态不完善
5. 导出功能表头重复
6. 导出记录时使用前端生成的产品编码
7. DetailModal 类型定义与全局类型不一致
8. 列表查询未正确处理 loading/error 状态
9. 品质等级硬编码
10. BatchEditModal 类型不匹配
11. generateHarvestCode 重复定义
12. 导出功能表头重复

### 育苗模块 - 10个

1. EditModal 缺失多个字段编辑
2. 生产计划关联代码问题
3. handleEnd 使用 window.location.reload()
4. 前端缺少API调用的loading状态
5. 字段映射可能丢失数据
6. API响应字段与前端期望不匹配
7. 用户信息硬编码
8. 字段映射临时补救措施
9. EditModal 缺失多个字段编辑
10. 生产计划关联代码问题

### 订单模块 - 12个

1. 类型导入路径不规范
2. 后端 stats 路由前端无感知
3. localStorage 并发问题
4. 未使用的导入
5. createBy 硬编码为"系统"
6. 订单状态选项重复定义
7. API 响应数据未充分利用
8. 字段映射不完整
9. 导出功能表头重复
10. 类型导入路径不规范
11. 后端 stats 路由前端无感知
12. localStorage 并发问题

### 仓储库存模块 - 12个

1. 物料新增功能未实现
2. 库存预警数据是 mock
3. 库存统计数据是 mock
4. 字段命名不一致 (Material vs InventoryRecord)
5. 后端 INSERT 存在 SQL 注入风险
6. 代码耦合严重 (1000+ 行 monolithic)
7. 硬编码价格单位
8. 入库审核流程未实现
9. 出库功能缺失
10. 物料新增功能未实现
11. 库存预警数据是 mock
12. 库存统计数据是 mock

### 人力资源模块 - 12个

1. 加班工资计算逻辑简化
2. 请假申请表单验证不完整
3. 审批流程 UI 状态同步问题
4. leaveQuotaService 配置硬编码
5. 员工数据从 localStorage 读取
6. 审批操作后 UI 立即更新但 API 可能失败
7. 请假额度与 leaveQuotaService 不同步
8. 加班计算使用默认工资 6000
9. 加班类型选项硬编码
10. 加班工资计算逻辑简化
11. 请假申请表单验证不完整
12. 审批流程 UI 状态同步问题

### 生产计划模块 - 12个

1. 每日/月度规划直接读取 localStorage
2. 后端 API 已实现但未被使用
3. 字段映射不一致
4. 作物阶段任务配置硬编码 (只支持3种作物)
5. 成本计算硬编码
6. 后端无生产计划CRUD API
7. 创建计划未持久化到SQLite
8. 每日/月度规划直接读取 localStorage
9. 作物阶段任务配置硬编码
10. 成本计算硬编码
11. 后端 SELECT 字段不完整
12. 前端无生产计划CRUD服务

### 农事任务模块 - 10个

1. 任务派发不调用后端 API
2. updateSeedling 缺少 await
3. API 数据合并可能丢失本地数据
4. useFarmHub 与 useTasks 数据竞争
5. recentRecords 数据源混乱
6. 操作记录与任务记录不同步
7. 任务确认不调用API
8. 任务统计不调用API
9. 任务归档不调用API
10. 任务派发不调用后端 API

### 审批工作流 - 10个

1. 审批联动API错误处理不完善
2. HrApproval 审批通过未传 comment
3. 审批联动触发器配置分散
4. 审批状态枚举不一致
5. 审批操作记录不完整
6. 审批超时处理缺失
7. 审批代理功能缺失
8. 批量审批API缺失
9. 审批历史查询缺失
10. 审批通知缺失

---

## 🟢 LOW 问题清单（共54个）

1. 删除操作没有等待完成 - SeedlingPage.tsx
2. useEffect 缺少依赖 - OrderPage.tsx
3. 筛选逻辑使用 includes 可能误匹配 - 多处
4. 类型断言过于宽松 - 多处
5. 导出功能未调用 API - HarvestPage.tsx
6. 库存服务导出但未使用 - inventoryService.ts
7. OvertimePage 默认工资硬编码 - 6000
8. 批次数据无后端 API - cropBatchService.ts
9. API 缺少请求超时配置
10. 缺少统一错误提示组件
11. 某些 modal 关闭后状态未清理
12. 表格排序使用本地状态而非 URL 参数
13. 缺少请求取消机制 (AbortController)
14. 日期选择器格式不统一
15. 缺少空状态组件
16. 加载状态 UX 不友好 (无骨架屏)
17. 移动端适配不完整
18. 缺少键盘快捷键支持
19. 缺少数据导出进度提示
20. 删除操作没有等待完成
21. useEffect 缺少 refreshData 依赖
22. 筛选逻辑使用 includes 可能导致误匹配
23. 类型断言过于宽松
24. 库存服务导出但未被使用
25. 导出功能未调用API获取完整数据
26. OvertimePage 中使用常量作为默认工资
27. 批次数据无后端API支持
28. grayScaleConfig 缺少模块默认配置处理
29. 多处使用 localStorage.getItem 未做空值处理
30. 缺少统一请求/响应拦截器
31. 缺少 API 错误统一处理机制
32. 多处 console.log 调试代码未清理
33. 缺少 API 请求超时处理
34. 缺少请求重试机制
35. 某些组件缺少 React.memo 优化
36. 某些组件重复渲染未优化
37. 缺少统一loading状态管理
38. 缺少统一分页组件
39. 缺少统一筛选组件
40. 缺少统一排序组件
41. 缺少统一导出组件
42. 缺少统一导入组件
43. 某些组件重复渲染未优化
44. 缺少统一loading状态管理
45. 缺少统一分页组件
46. 缺少统一筛选组件
47. 缺少统一排序组件
48. 缺少统一导出组件
49. 缺少统一导入组件
50. 导出功能表头重复
51. generateHarvestCode 重复定义
52. BatchEditModal 类型不匹配
53. 品质等级硬编码
54. 列表查询 loading/error 状态不完善

---

## 📋 数据流混乱分布图

```
模块                    localStorage    后端API     问题数     状态
────────────────────────────────────────────────────────────────────
库存(Warehouse)         ⚠️ Shadow     ❌ 未调用   12 CRITICAL 🔴 危险
薪资(Salary)            ✅ 100%       ❌ 无表     2 CRITICAL  🔴 危险
加班(Overtime)         ✅ 100%       ❌ 无表     3 CRITICAL  🔴 危险
请假(Leave)             ✅ 100%       ⚠️ 部分     3 CRITICAL  🔴 危险
批次(Batch)            ⚠️ 回退       ❌ 无API    4 CRITICAL  🔴 危险
物料(Material)         ⚠️ 全部      ❌ 未调用   6 CRITICAL  🔴 危险
育苗(Seedling)         ⚠️ 回退       ⚠️ 部分     6 CRITICAL  🔴 危险
种植(Planting)         ⚠️ 回退       ⚠️ 部分     6 CRITICAL  🔴 危险
采收(Harvest)         ⚠️ 回退       ⚠️ 部分     5 CRITICAL  🔴 危险
订单(Order)            ⚠️ 回退       ⚠️ 部分     4 CRITICAL  🔴 危险
农事任务(Task)         ✅ 100%       ❌ 未调用   4 CRITICAL  🔴 危险
部门(Department)       ✅ Shadow     ⚠️ 部分     -          🟡 混乱
仓库(Warehouse)        ✅ Shadow     ⚠️ 部分     -          🟡 混乱
温室(Greenhouse)       ✅ Shadow     ⚠️ 部分     -          🟡 混乱
权限(Authority)        ✅ Shadow     ⚠️ 部分     -          🟡 混乱
────────────────────────────────────────────────────────────────────
总计                   6个模块       7个模块     165+ CRITICAL
                        100%依赖LS    API未调用
```

---

## 🔧 修复优先级建议

### 阶段一：止血（1-2天）P0

| 优先级 | 模块 | 任务 | 涉及文件 |
|--------|------|------|---------|
| P0 | 种植 | 修复 EditModal.handleSubmit | EditModal.tsx:38-41 |
| P0 | 种植 | 实现后端采收路由 | planting.ts |
| P0 | 采收 | 修复 harvestService 调用 | HarvestPage.tsx:550 |
| P0 | 采收 | 实现批量编辑/删除 API | harvest.ts |
| P0 | 育苗 | 修复 SeedlingPage 筛选 bug | SeedlingPage.tsx:180 |
| P0 | 育苗 | 实现每日/定植/打印 API | seedling.ts |
| P0 | 库存 | 实现入库 API 调用 | WarehouseMaterialsPage.tsx |
| P0 | 人力 | 实现加班记录持久化 | OvertimePage.tsx |
| P0 | 人力 | 修复所有 await 缺失 | LeavePage.tsx, OvertimePage.tsx |
| P0 | 生产 | 实现生产计划 API 调用 | ProductionPage.tsx |

### 阶段二：数据迁移（3-5天）P1

| 优先级 | 任务 | 说明 |
|--------|------|------|
| P1 | 创建缺失的后端表 | daily_records, print_records, transplant_records |
| P1 | 补充后端业务 API | seedling (20+), planting (5), harvest (3) |
| P1 | 统一数据源 | 移除 USE_API 开关，强制使用后端 API |
| P1 | 创建种子数据脚本 | 为所有表填充初始数据 |
| P1 | 迁移 shadow 模式模块 | department, warehouse, greenhouse |

### 阶段三：清理（1-2天）P2

| 优先级 | 任务 |
|--------|------|
| P2 | 移除硬编码 Mock 数据 |
| P2 | 完善错误处理和 loading 状态 |
| P2 | 清理废弃的 localStorage 服务 |
| P2 | 实现统一的数据获取 hook |
| P2 | 添加 API 请求拦截器和错误处理 |
| P2 | 拆分 monolithic 组件 |

---

## 🎯 核心结论

### 系统当前状态：无法用于生产

1. **数据持久化严重缺失**：大部分模块的操作只更新内存状态，不调用后端 API
2. **双数据源混乱**：localStorage 和后端 API 同时存在，但不同步
3. **后端 API 不完整**：已实现的 API 只有 30%，70% 的业务 API 缺失
4. **硬编码数据泛滥**：系统依赖大量 mock 数据和硬编码值
5. **安全漏洞严重**：无鉴权、SQL注入、密码明文存储等问题

### 最高优先修复项

1. **修复 harvestService vs apiHarvestService 调用错误** (影响所有采收操作)
2. **修复 EditModal.handleSubmit** (影响种植编辑)
3. **实现后端缺失的 20+ API** (影响育苗核心功能)
4. **修复 OvertimePage 加班记录持久化** (影响薪资计算)
5. **修复 WarehouseMaterialsPage 入库 API** (影响库存管理)
6. **实现 JWT 认证中间件** (安全漏洞)
7. **修复 SQL 注入漏洞** (安全漏洞)

---

## 📊 统计汇总

| 严重程度 | 数量 |
|---------|------|
| CRITICAL | 165+ |
| HIGH | 183+ |
| MEDIUM | 130+ |
| LOW | 54+ |
| **总计** | **532+** |

---

**报告生成时间**: 2026-05-05
**审查版本**: V5.0（综合 opencode V1.0 + V1.0 + V2.0 + V3.0 + V4.0）
**审查执行**: 31个专业代码审查代理并行审查
**审核文件数**: 200+ 个核心文件
**下次审查建议**: 修复 CRITICAL 问题后进行复查
