# 深度代码审查报告 - 智慧种植生产管理系统

**审查时间**：2026-04-17
**审查范围**：`D:\TMcrop\yuanxingtu\V1.1\src`
**审查性质**：只读审查，不修改任何文件
**审查方式**：20个专业代理并行深度审查

---

## 一、整体评价

**项目技术栈**：React 18 + TypeScript + Vite + Tailwind CSS + Radix UI

| 维度 | 评分 | 说明 |
|------|------|------|
| 架构设计 | 7/10 | 结构清晰，但组件过大 |
| 代码质量 | 5/10 | 存在硬编码、XSS 风险、内存泄漏 |
| 可维护性 | 4/10 | 文件过大（最高3844行），重复代码多 |
| 安全性 | 4/10 | 存在 XSS 漏洞、内存泄漏 |
| 类型安全 | 7/10 | TypeScript 使用良好，但有类型冲突 |
| UI一致性 | 5/10 | 弹窗实现不统一，交互体验差 |

---

## 二、CRITICAL 问题清单（必须立即修复）

### 1. 【CRITICAL】Hooks 内存泄漏

**文件**：
- `src/hooks/useGlobalDraggable.ts`
- `src/hooks/useGlobalDraggableV2.ts`

**问题**：
- 模块级 `Map` 存储状态，违反 React 状态管理原则
- `dragStateMap` 和 `globalDragState` 永不清理
- 直接修改 `state.isDragging = true` 绕过 React 状态机制

**影响**：每次拖拽都会泄漏内存，长时间使用会导致浏览器崩溃

---

### 2. 【CRITICAL】XSS 安全漏洞（24+ 处）

**涉及文件**：
- `src/components/purchasePlan/PurchasePlanPage.tsx`（第 239、326、393 行）
- `src/components/production/ProductionPage.tsx`（第 239、243 行）
- `src/components/supplier/SupplierManagementPage.tsx`（第 220、231 行）
- `src/components/hrAttendance/HrAttendancePage.tsx`（第 79、83 行）
- `src/components/farm/taskDispatch/TaskDispatchPage.tsx`（第 1392、1396 行）
- `src/components/labor/worklog/WorkLogPage.tsx`（第 237、241 行）
- `src/components/labor/performance/PerformancePage.tsx`（第 131、135 行）
- `src/components/labor/overtime/OvertimePage.tsx`（第 187、191 行）
- `src/components/labor/leave/LeavePage.tsx`（第 187、191 行）
- `src/components/labor/tempWorker/TempWorkerPage.tsx`（第 244、248 行）
- `src/components/labor/contract/ContractTable.tsx`（第 344、348 行）
- `src/components/labor/onboarding/OnboardingPage.tsx`（第 309、313 行）
- `src/components/labor/piecework/PieceworkPage.tsx`（第 278、282 行）
- `src/components/farm/harvest/HarvestPage.tsx`（第 121、126 行）
- `src/pages/Materials.tsx`（第 427、431 行）
- `src/pages/MaterialReceiving.tsx`（第 986、988、992 等多行）
- `src/pages/Reports.tsx`（第 144、152 行）
- `src/components/summary/useExport.ts`（第 75、244 行）

**问题**：导出功能 HTML 拼接未转义
```typescript
content = `<html>...<td>${row[h] || ''}</td>...</html>`
```

**影响**：攻击者可在物料名称、备注等字段中注入恶意脚本

---

### 3. 【CRITICAL】React key 缺失

**文件**：
- `src/components/materialReceiving/ApplicationTab.tsx`（第 414、472 行）
- `src/components/materialReceiving/ExecuteTab.tsx`（第 400、455 行）
- `src/components/materialReturn/MaterialReturnTable.tsx`（第 65 行）

**问题**：map 中的 Fragment 没有 key props
```tsx
// 错误
filteredData.map((item) => (<>
  <tr>...</tr>
</>))

// 正确
filteredData.map((item) => (<React.Fragment key={item.id}>
  <tr>...</tr>
</React.Fragment>))
```

---

### 4. 【CRITICAL】DragContext useState 错误使用

**文件**：`src/components/DragContext.tsx`

**问题**：
- 第 18 行：`useState` 返回值被当作普通对象使用
- 第 20-25 行：`getDragHandlers` 是空实现，Context 无实际功能

---

### 5. 【CRITICAL】分页逻辑未实现

**文件**：`src/components/baseSettings/BaseSettingsPage.tsx`

**问题**：定义了分页状态但渲染时没有切片
```tsx
const [currentPage, setCurrentPage] = useState(1);
const [pageSize, setPageSize] = useState(10);
const totalPages = Math.ceil(totalItems / pageSize);
// 但渲染时没有切片！
filteredData.map((item) => ...)
```

---

### 6. 【CRITICAL】数据未实际保存

**文件**：`src/components/purchasePlan/PurchasePlanPage.tsx`

**问题**：`handleCreateSubmit` 只关闭弹窗，未保存数据
```tsx
const handleCreateSubmit = () => {
  setShowCreateModal(false);
  // 缺少：保存数据的逻辑
};
```

---

### 7. 【CRITICAL】HTML 结构错误

| 文件 | 问题 |
|------|------|
| `src/components/labor/salary/SalaryTable.tsx`（第 264 行） | 多余 `</div>` |
| `src/pages/MaterialReceiving.tsx` | 186 个开标签 / 183 个闭标签，3 个 div 未闭合 |
| `src/components/supplier/SupplierEditModal.tsx`（第 306-308 行） | 多余闭合标签 |
| `src/components/cost/CostPage.tsx` | div 标签不匹配 |

---

### 8. 【CRITICAL】批量选择使用索引（分页后选错记录）

**文件**：`src/components/farm/harvest/HarvestPage.tsx`

**问题**：
```tsx
const [selectedRows, setSelectedRows] = useState<number[]>([]); // 存储索引
const selectedData = filteredRecords.filter((_, index) => selectedRows.includes(index));
// 分页后 filteredRecords 只包含当前页，但 selectedRows 存储的是全局索引
```

---

## 三、HIGH 问题清单

### 1. 【HIGH】搜索按钮无效

**文件**：`src/components/production/ProductionPage.tsx`（第 405 行）
```tsx
onSearch={() => {}} // 空函数，点击无任何效果
```

---

### 2. 【HIGH】全选逻辑错误

**文件**：`src/components/labor/attendance/WorkerAttendanceTable.tsx`（第 54 行）
```tsx
const allSelected = data.length > 0 && selectedRows.length === totalCount;
// totalCount 是全部数据量，但表格只显示当前页
```

---

### 3. 【HIGH】直接修改 mock 数据

| 文件 | 位置 |
|------|------|
| `src/components/labor/salary/useSalary.ts` | 第 241-246 行 |
| `src/components/labor/piecework/usePiecework.ts` | 第 223-232 行 |
| `src/components/farm/harvest/HarvestPage.tsx` | 批量删除索引计算错误 |

---

### 4. 【HIGH】硬编码问题汇总

| 文件 | 位置 | 硬编码内容 |
|------|------|-----------|
| PurchasePlanPage.tsx | 88-89, 200-201 | '陆启闯', '生产部' |
| PurchasePlanPage.tsx | 1024-1027 | 批次号 SC202603001 等 |
| ProductionPage.tsx | 57, 148, 168 | '陆启闯' |
| ProductionPage.tsx | 28-30 | 武侠人物名：郭靖、黄蓉、张无忌 |
| MaterialReceivingPage.tsx | 193-194 | '仓库A区', '王志刚' |
| materialReturn | 多处 | 武侠人物名：李建国、王建华等 |

---

### 5. 【HIGH】useEffect 依赖不完整

| 文件 | 位置 | 问题 |
|------|------|------|
| PurchasePlanPage.tsx | 128-138 行 | 缺少 `setEditedPlans` |
| TasksPage.tsx | 133 行 | 缺少 `updateProblem` |
| supplier/SupplierEditModal.tsx | 37-61 行 | 缺少 `isOpen` |
| useTasks.ts | 360-367 行 | 缺少 `DATA_VERSION` |

---

### 6. 【HIGH】MaterialItem 类型冲突

**文件**：
- `src/types/materialReceiving.ts`
- `src/components/materialReturn/types.ts`

两个文件的 `MaterialItem` 接口结构完全不同但名称相同

---

### 7. 【HIGH】表格列数不匹配

| 文件 | 表头列数 | 展开行列数 |
|------|---------|-----------|
| ApplicationTab.tsx | 12 列 | 10 列 |
| ExecuteTab.tsx | 11 列 | 13 列 |
| CostBatchTable.tsx | 8 列 | colSpan={7} |

---

### 8. 【HIGH】批量删除索引计算错误

**文件**：`src/components/farm/harvest/HarvestPage.tsx`（第 247-259 行）
```tsx
const indicesToDelete = new Set<number>();
// indicesToDelete 是 Set，应该用 has() 判断，但逻辑有问题
```

---

## 四、MEDIUM 问题清单

### 1. 【MEDIUM】弹窗遮罩关闭功能缺失

所有自定义弹窗（DeleteConfirm, VoidModal, EditWarningModal 等）无点击遮罩关闭功能

### 2. 【MEDIUM】表单验证缺失

- `SupplierAddModal.tsx`、`SupplierEditModal.tsx`：无任何验证
- `materialReceiving/AddModal.tsx`、`EditModal.tsx`：无必填验证

### 3. 【MEDIUM】拖拽组件无边界限制

DraggableDialog、DraggableModalWrapper 拖拽时可以超出视口

### 4. 【MEDIUM】ExecuteTabWrapper 状态隔离

`MaterialReceivingPage.tsx` 中 ExecuteTabWrapper 状态与主状态隔离，可能导致数据不一致

### 5. 【MEDIUM】TabHeader 缺少 ARIA 属性

无障碍属性 `role="tab"`、`aria-selected`、`aria-controls`

### 6. 【MEDIUM】SkillTag 类型缺失

`types/index.ts` 中引用了 `SkillTag` 类型但未定义

### 7. 【MEDIUM】useState 错误使用

`DragContext.tsx` 中 `useState` 返回值被当作普通对象

### 8. 【MEDIUM】数据一致性问题

| 文件 | 问题 |
|------|------|
| costData.ts | KPI数据与月度趋势数据不一致 |
| materialReceivingData.ts | getCategoryByCode 缺少分类 |
| materialReturn/mockData.ts | 武侠人名与真实人名混用 |
| warehouseMaterials_old/mockData.ts | price 字段格式不统一 |

---

## 五、LOW 问题清单

| 问题 | 文件 | 说明 |
|------|------|------|
| console.log 调试语句 | 多个文件 | 生产环境应移除 |
| ErrorBoundary 拼写错误 | ErrorBoundary.tsx:3 | `searilizeError` → `serializeError` |
| 日期格式化时区问题 | LaborFilters.tsx:77 | `toISOString()` 导致时区问题 |
| 翻译冗余 | translations.ts:195 | `aboutSubtitle` 与 `aboutTitle` 相同 |
| LaborStatCard 图标背景色硬编码 | LaborStatCard.tsx:94 | 固定 emerald 色系 |
| window.confirm/alert 不一致 | Labor 模块多个页面 | 应统一使用 Toast |

---

## 六、模块审查详情

### 采购计划模块
- CRITICAL: handleCreateSubmit 未保存数据
- HIGH: 硬编码人员/部门/批次号
- HIGH: useEffect 依赖缺失
- MEDIUM: 导出数据字段名不匹配

### 生产管理模块
- CRITICAL: 搜索按钮 onSearch 是空函数
- HIGH: 负责人列表硬编码武侠人物名
- MEDIUM: 多个弹窗未复用 Modal 组件

### 物料接收模块
- CRITICAL: React key 缺失（Fragment 无 key）
- CRITICAL: 表格列数不匹配
- HIGH: ExecuteTabWrapper 状态隔离
- MEDIUM: 所有自定义弹窗无遮罩关闭

### 供应商管理模块
- CRITICAL: XSS 漏洞（导出 HTML 拼接）
- HIGH: SupplierEditModal HTML 结构错误
- HIGH: 组织名称硬编码
- MEDIUM: 表单验证缺失

### 系统设置模块
- CRITICAL: 分页逻辑未实现
- HIGH: 表单使用原生 DOM 获取值
- HIGH: 编辑时部分字段未更新

### 人工管理模块
- CRITICAL: SalaryTable HTML 语法错误
- CRITICAL: 全选逻辑使用 totalCount 非当前页
- CRITICAL: 直接修改 mock 数据
- HIGH: useEffect 依赖不完整

### 农事记录模块
- CRITICAL: HarvestPage 批量选择使用索引，分页后选错
- HIGH: 批量删除索引计算错误
- MEDIUM: 使用 window.location.reload()

### 成本分析模块
- CRITICAL: div 标签不匹配（186 开/183 闭）
- HIGH: colSpan 不匹配
- HIGH: 领料次数 +4 硬编码
- MEDIUM: costDiffRate 硬编码

### Hooks（第一部分）
- CRITICAL: useGlobalDraggable 内存泄漏
- CRITICAL: useGlobalDraggableV2 内存泄漏
- CRITICAL: 状态直接修改绕过 React
- HIGH: 返回值不完整

### Hooks（第二部分）
- HIGH: usePiecework 直接修改 mock 数据
- MEDIUM: useOvertime 返回未过滤数据
- MEDIUM: useContract 返回值类型不一致

### 公共组件
- HIGH: LaborTable Checkbox 使用 ref 操作 DOM
- HIGH: LaborFilters SelectItem value 逻辑错误
- MEDIUM: TabHeader 缺少 ARIA 属性

### 类型定义
- HIGH: MaterialItem 类型冲突
- MEDIUM: SkillTag 类型缺失
- MEDIUM: priority 字段中英文不一致

### MockData
- HIGH: 武侠人名用于正式数据
- MEDIUM: 日期跨年度不一致（2024 vs 2026）
- MEDIUM: 部门/分类选项硬编码

### 工具函数
- LOW: translations.ts 英文翻译冗余

### 布局组件
- CRITICAL: DragContext useState 错误使用
- CRITICAL: getDragHandlers 空实现
- LOW: Header Fragment 格式不规范

### 其他模块（Indicators, Announcement, ParkArchive 等）
- MEDIUM: Indicators yearFilter 未使用
- MEDIUM: 多个页面缺少搜索/弹窗功能

---

## 七、优先修复顺序

| 优先级 | 问题 | 涉及模块 |
|--------|------|----------|
| P0 | Hooks 内存泄漏（useGlobalDraggable/V2） | Hooks |
| P0 | XSS 安全漏洞（24+ 文件） | 采购、供应商、生产、Labor 等 |
| P0 | React key 缺失 | 物料接收、物料退货 |
| P0 | 分页未实现 | 系统设置 |
| P0 | 数据未保存 | 采购计划 |
| P0 | HTML 结构错误 | 成本、人工、供应商 |
| P0 | 批量选择索引错误 | 农事 |
| P1 | 搜索按钮无效 | 生产管理 |
| P1 | 全选逻辑错误 | 人工管理 |
| P1 | 直接修改 mock 数据 | 人工、Labor |
| P1 | 硬编码问题 | 多个模块 |
| P1 | useEffect 依赖缺失 | 多个模块 |
| P2 | 弹窗统一性 | 多个模块 |
| P2 | 表单验证 | 多个模块 |
| P2 | 拖拽边界 | 布局组件 |

---

## 八、值得学习的地方

1. **类型定义规范**：`src/types/purchase.ts` 类型定义清晰，使用 `as const` 固定枚举
2. **数据持久化**：`useLocalStorage` hook 实现良好
3. **配置外置**：`TASK_STATUS_CONFIG` 等配置常量独立管理
4. **状态管理分离**：将业务逻辑封装在 hook 中
5. **utils.ts**：使用 clsx + tailwind-merge 标准组合

---

## 九、总结

本次深度审查由 **20 个专业代理并行审查** 完成，共发现：

| 严重程度 | 数量 |
|----------|------|
| CRITICAL | 8 项核心问题（影响功能可用性） |
| HIGH | 15+ 项（影响数据安全和正确性） |
| MEDIUM | 20+ 项（影响代码质量和维护性） |
| LOW | 15+ 项（影响代码规范） |

**最优先修复项**：
1. Hooks 内存泄漏 - 导致浏览器崩溃
2. XSS 安全漏洞（24+ 处）- 导致恶意脚本注入
3. 分页/搜索功能无效 - 功能完全不能用
4. HTML 结构错误 - 导致布局错乱
5. 数据未保存 - 用户数据丢失

---

## 第二阶段：业务流程深度审查（2026-04-17）

审查了6个核心审批流程，发现多个CRITICAL级别的流程失效问题。

---

### 一、农事任务分派流程

**文件**：`src/components/farm/taskDispatch/TaskDispatchPage.tsx`（3844行）

#### 状态流转
```
draft → pending → accepted → in_progress → waiting_acceptance → completed
```

#### CRITICAL问题

| 问题 | 位置 | 影响 |
|------|------|------|
| 数据双写同步风险 | TaskDispatchPage.tsx:1392-1396 | 两处调用saveTask，第二个失败会导致数据不一致 |
| ID格式不统一 | TaskDispatchPage.tsx | 部分用number，部分用string |
| 权限检查未执行 | TaskDispatchPage.tsx | `canDispatch`函数定义但未使用 |

#### HIGH问题

| 问题 | 位置 |
|------|------|
| 状态变更后未刷新列表 | useTasks.ts |
| 验收操作触发条件判断错误 | TaskDispatchPage.tsx |
| 缺少操作日志记录 | 整个模块 |

---

### 二、临时任务分派执行流程

**文件**：`src/components/farm/tempTask/`

#### CRITICAL问题

```typescript
// TempWorkerPage.tsx 第244-248行 - handleReassign 调用了未定义的 updateTempTask
const handleReassign = (task: TempTask) => {
  updateTempTask(task.id, {  // updateTempTask 未从 hook 解构！
    status: 'pending',
  });
};
```

**问题**：编辑和删除操作是空壳函数，点击无任何效果

| 操作 | 状态 | 问题 |
|------|------|------|
| 新增 | 正常 | - |
| 编辑 | 空壳 | 点击无效果 |
| 删除 | 空壳 | 点击无效果 |
| 重新分派 | 调用未定义方法 | 抛出错误 |

---

### 三、巡查问题分派执行流程

**文件**：`src/components/inspection/`

#### 状态流转

```
上报问题(report) → 待处理 → 分派任务(dispatch) → 处理中 → 提交反馈(submit) → 待验收 → 验收通过(approve) → 已处理
                                                      ↓
                                               返工(reject_acceptance)
```

#### CRITICAL问题

| 问题 | 位置 | 说明 |
|------|------|------|
| **任务状态与问题状态不同步** | useProblemDispatch.ts | 分派后任务状态一直是pending，只有验收通过才更新 |
| **acceptProblem函数无实际作用** | useProblemDispatch.ts:265 | 接单后问题和任务状态都不变 |

**关键Bug**：执行人开始处理→提交反馈→所有操作都不更新任务状态，只有验收通过才同步

```typescript
// 问题状态变"处理中"但关联任务状态仍是 pending
dispatchProblem() {
  const newTask: Task = {
    status: 'pending',  // 问题分派了，任务状态没有跟随变为 in_progress
  };
}

// submitProblemFeedback 也不更新任务状态
submitProblemFeedback() {
  updateProblem(problemId, { status: '待验收' });
  // 但关联任务状态没有更新为 waiting_acceptance！
}

// 唯一更新点：验收通过才同步
approveProblemCompletion() {
  if (problem.sourceTaskId) {
    setTasks(prev => prev.map(t =>
      t.id === problem.sourceTaskId ? { ...t, status: 'completed' } : t
    ));
  }
}
```

#### MEDIUM问题

| 问题 | 位置 |
|------|------|
| addProgressRecord状态判断错误（fromStatus和toStatus都是'处理中'） | useProblemDispatch.ts:380 |
| 验收入口不清晰 | InspectionPage.tsx |
| reject_acceptance流转显示缺失 | TaskFlowTimeline.tsx |

---

### 四、采购计划审批流程

**文件**：`src/components/purchasePlan/PurchasePlanPage.tsx`

#### 状态流转
```
新建 → 待审批 → 已审批/已驳回
```

#### CRITICAL问题

```typescript
// handleCreateSubmit 只关闭弹窗，没有保存数据！
const handleCreateSubmit = () => {
  setShowCreateModal(false);  // 数据丢失！
};
```

| 问题 | 严重程度 | 说明 |
|------|----------|------|
| handleCreateSubmit不保存数据 | CRITICAL | 新增采购计划数据全部丢失 |
| 缺少审批处理函数 | CRITICAL | approvePlan/approveMultiPlan 未实现 |
| 缺少驳回处理函数 | HIGH | rejectPlan 函数体为空 |

---

### 五、物料入库领取退料审批流程

**文件**：`src/components/materialReceiving/`, `src/components/materialReturn/`

#### 状态流转

**入库流程**：
```
申请 → 审批通过 → 入库（但无库存更新！）
```

**领取流程**：
```
申请 → 审批通过 → 领取（但无库存扣减！）
```

**退料流程**：
```
申请 → 审批通过 → 退料（但无库存归还！）
```

#### CRITICAL问题

| 问题 | 位置 | 说明 |
|------|------|------|
| **库存模块完全未实现** | 整个模块 | 入库不入库存，领取不减库存，退料不还库存 |
| 入库操作无库存更新 | MaterialReceivingPage.tsx | 物料入库后，库存数据无任何变化 |
| 领取操作无库存更新 | MaterialReceivingPage.tsx | 物料被领取后，库存数据无任何变化 |
| 退料操作无库存更新 | MaterialReturnPage.tsx | 物料退料后，库存数据无任何变化 |

**库存业务完全缺失**：没有实际的库存增减逻辑，整个物料管理系统与库存系统脱节

---

### 六、人事管理流程

**文件**：`src/components/labor/leave/`, `src/components/labor/overtime/`, `src/components/labor/salary/`

#### 状态流转

**请假流程**：
```
新建 → 待审批 → 已审批/已驳回/已取消
```

**加班流程**：
```
新建 → 待审批 → 已审批/已驳回/已取消
```

#### CRITICAL问题

| 模块 | 函数 | 问题 |
|------|------|------|
| 请假 | handleSave | 只关闭弹窗，不保存数据 |
| 请假 | handleApprove/handleReject | 只关闭弹窗，不更新状态 |
| 加班 | handleSave | 创建记录但不添加到状态数组 |
| 加班 | handleApprove/handleReject | 只关闭弹窗，不更新状态 |

```typescript
// useLeave.ts - handleSave 实际行为
const handleSave = useCallback((data: Partial<LeaveRecord>) => {
  setIsFormOpen(false);  // 只关闭弹窗！没有 setLeaveRecords 调用
}, []);

// useOvertime.ts - handleSave 实际行为
const handleSave = useCallback((data: OvertimeFormData) => {
  const newRecord: OvertimeRecord = { ... };  // 创建了记录
  console.log('保存加班记录:', newRecord);  // 只打印日志
  setIsFormOpen(false);  // 但没有 setOvertimeRecords 调用！
}, []);
```

#### HIGH问题

| 问题 | 位置 |
|------|------|
| updateRecordStatus直接修改原对象，违反不可变性 | useSalary.ts:241-246 |
| 离职流程不完整，只有状态标记 | personnel模块 |

---

### 七、第二阶段问题汇总

| 严重程度 | 数量 | 主要问题 |
|----------|------|----------|
| CRITICAL | 10+ | 数据不保存/不更新、库存系统缺失、状态同步失效 |
| HIGH | 12+ | 编辑删除空壳、权限检查未执行、违反不可变性 |
| MEDIUM | 8+ | 流转记录错误、验收入口不清晰 |

---

### 八、第二阶段优先修复顺序

| 优先级 | 问题 | 涉及模块 |
|--------|------|----------|
| P0 | **库存模块完全缺失** | 物料入库/领取/退料 |
| P0 | **请假数据不保存** | 人事-请假 |
| P0 | **加班数据不保存** | 人事-加班 |
| P0 | **任务状态不同步** | 巡查问题分派 |
| P0 | **临时任务空壳函数** | 临时任务 |
| P0 | **采购计划数据丢失** | 采购计划 |
| P1 | 权限检查未执行 | 农事任务分派 |
| P1 | 驳回处理函数为空 | 采购计划 |
| P1 | 离职流程不完整 | 人事-离职 |
| P2 | 验收入口不清晰 | 巡查问题分派 |
| P2 | 流转记录显示错误 | 巡查问题分派 |

---

**第二阶段审查完成时间**：2026-04-17
**第二阶段审查方式**：6个专业代理并行深度审查
**第二阶段审查范围**：农事任务分派、临时任务分派执行、巡查问题分派执行、采购计划审批、物料入库领取退料、人事管理流程
