# P2 AI 模块真实度审核报告

- 审核日期：2026-09-02
- 审核对象：AI-08、AI-07、AI-14、AI-15
- 审核方式：完整读取 8 个目标文件；只读检查路由装配、表结构、当前数据库数据；只读调用四个 Service 的默认场景
- 代码变更：未修改源代码、数据库或现有配置文件；本文件是本次唯一新增的 Markdown 报告

## 1. 范围与文件清单

本次 8 个目标文件对应如下：

1. `D:/TMcrop/yuanxingtu/V1.1/server/src/routes/ai/route.ts`
2. `D:/TMcrop/yuanxingtu/V1.1/server/src/services/ai/routeOptimizer.ts`
3. `D:/TMcrop/yuanxingtu/V1.1/server/src/routes/ai/resource.ts`
4. `D:/TMcrop/yuanxingtu/V1.1/server/src/services/ai/resourceOptimizer.ts`
5. `D:/TMcrop/yuanxingtu/V1.1/server/src/routes/ai/anomaly.ts`
6. `D:/TMcrop/yuanxingtu/V1.1/server/src/services/ai/anomaly.ts`
7. `D:/TMcrop/yuanxingtu/V1.1/server/src/routes/ai/attendance.ts`
8. `D:/TMcrop/yuanxingtu/V1.1/server/src/services/ai/attendance.ts`

源码注释对优先级的标注并不完全一致：AI-07、AI-08 标为 P1，AI-14、AI-15 标为 P2。本报告按用户指定的四个模块审计，优先级标签不作为真实性证据。

## 2. 评分口径

评分不是成熟度宣传值，按当前实现逐项扣分：

- 算法真实性（0-3）：没有算法为 0；固定模板或演示逻辑为 1；真实可运行的基础算法为 2；有模型训练、验证、异常输入处理和业务约束为 3。
- 数据真实查询（0-3）：不查数据库为 0；只查单表或硬编码数据为 1；多表真实聚合但存在关键口径错误为 2；数据链路、过滤、分母和时效均经过验证为 3。
- 业务逻辑完整性（0-3）：只覆盖单一演示场景为 1；覆盖主流程但缺少约束、分母、告警闭环为 2；形成可执行的完整业务闭环为 3。
- 返回结果可用性（0-1）：只有空壳或不可消费结果为 0；有结构化结果但缺少必要上下文、边界或可靠性说明为 0.5-0.8；可直接驱动业务且带完整解释为 1。

## 3. 总评分

| 模块 | 算法真实性 | 数据真实查询 | 业务逻辑完整性 | 返回结果可用性 | 总分 | 结论 |
|---|---:|---:|---:|---:|---:|---|
| AI-08 路径优化 | 2.0 | 0.5 | 1.5 | 0.7 | **4.7/10** | 真实可运行的启发式算法，但未接业务数据，且没有约束优化 |
| AI-07 资源优化 | 0.5 | 1.5 | 1.0 | 0.6 | **3.6/10** | 规则式库存预警，不是预测或采购优化器 |
| AI-14 异常检测 | 1.0 | 1.5 | 0.8 | 0.5 | **3.8/10** | 多表查询真实存在，但算法声明失真、结果在当前数据下为空 |
| AI-15 出勤异常 | 0.5 | 1.3 | 0.7 | 0.5 | **3.0/10** | 固定阈值的规则筛查，未实现所声明的 Z-score 评分 |

综合平均分为 **3.8/10**。四个模块更准确的定位是：启发式路径、库存规则、统计原型、考勤规则筛查；不能按当前代码称为经过验证的机器学习 AI 系统。

## 4. 当前数据库与默认调用证据

本次对 `D:/TMcrop/yuanxingtu/V1.1/server/data/yuanxingtu.db` 做只读检查，完整性检查结果为 `ok`。读取到的工作库数据如下：

| 数据对象 | 当前数量或状态 | 对审核的意义 |
|---|---:|---|
| `farm_tasks` | 51 行；仅 1 行 `actual_hours > 0`；11 行有 `completed_at`；`completion_date` 为 0 | 默认 30 天窗口内没有足够的任务时长基线，任务耗时检测无法产生有效分组 |
| `harvest_records` | 164 行；仅 36 行 `harvest_quantity > 0` | 默认 30 天内没有达到代码要求的 3 条/作物分组，产量检测当前为 0 个检查组 |
| `inventory_stock` | 88 行；`in_stock` 47、`low_stock` 3、`empty` 2、`frozen_full` 1、`frozen_partial` 6、`transferred` 29 | AI-07 只查 `in_stock`，会漏掉库存告急、空库存和冻结库存 |
| `inventory_transaction` | 388 行；`outbound` 53 行 | 默认 30 天内没有任何物料达到 5 条出库流水基线，库存消耗检测当前为 0 个检查组 |
| `attendance_records` | 23 行；19 个不同 `worker_id`；仅 7 个不同日期 | 样本跨度过短且员工 ID 与员工主表不一致 |
| `employees` | 6 行，全部为 `active` | AI-15 默认扫描 6 名员工，但当前考勤记录没有对应到这 6 名员工 |
| `greenhouses` | 21 行，仅 9 行 `lat` 和 `lng` 同时非 0 | AI-08 的真实派工入口对坐标缺失很敏感；只有部分任务能提供有效坐标 |
| 今日 `farm_tasks` | 0 行处于可优化状态 | 当前工作库没有可直接完成端到端路径演示的任务 |

只读调用默认输入 `{}` 的结果：

- AI-07：`total_materials=47`，`alerts=[]`，`summary={ok:47, low:0, critical:0, out:0}`，建议成本为 0。
- AI-14：`total_checks=0`，`anomalies=[]`，`summary={warning:0, critical:0}`。
- AI-15：`total_employees=6`，`anomalies=[]`，`summary={low:0, medium:0, high:0}`。
- AI-08 用 3 个任意坐标任务直接调用，原距离 9.50 km、优化距离 8.78 km、节省 7.6%；该值低于文件头声明的 15% 目标，但代码没有阻断、告警或质量标记。任意样本不能代表生产平均值，只能证明目标未被执行和验证。

当前 UI 会把 `[]` 显示为无异常或库存充足，但这只能说明检测逻辑没有找到候选数据，不能证明系统健康，更不能证明达到 PPT 中宣称的检出率或误报率。

## 5. 逐模块审核

### 5.1 AI-08 路径优化

#### 已实现的内容

- 入口是受认证保护的 `POST /api/ai/route/optimize`，路由壳在 `routes/ai/route.ts:12-25`。
- 距离计算使用 Haversine 公式，代码在 `routeOptimizer.ts:43-53`。
- 构建任务间距离矩阵在 `routeOptimizer.ts:56-70`。
- 最近邻初始解在 `routeOptimizer.ts:85-107`。
- 2-opt 局部改进在 `routeOptimizer.ts:109-138`，能降低简单样本的路径长度。
- 输出顺序、每步距离、累计距离、原始距离、节省比例、算法名和版本在 `routeOptimizer.ts:161-187`。

#### 主要不足与差距

1. **核心 Service 不查数据库。** `routeOptimizer.ts` 只有纯函数和输入体，不导入 `getDatabase`；路由 `route.ts:14-21` 只检查 `worker_start`、任务数组和非空。因此直接调用 AI-08 可以接受任意坐标，服务本身的数据真实性为 0。UI 侧确实会先调用 `dispatch/worker-tasks-and-location`，但那属于另一个端点的数据接入，不是本 Service 的真实查询。
2. **没有输入边界。** `route.ts:15-20` 不验证 `lat`/`lng` 是否为有限数、是否在经纬度范围、任务 ID 是否唯一、任务数量是否有上限。非法坐标可传播 `NaN`；大任务集合会产生 O(n²) 距离矩阵和内存风险。
3. **`original_order` 静默丢弃无效项。** `routeOptimizer.ts:151-154` 只要数组长度等于任务数就使用它，却把查找不到的 ID 过滤掉。未知 ID 或重复 ID 会让比较路径变短，代码不报 400，也不指出原顺序不完整。
4. **只是欧氏空间直线距离。** 没有道路网络、园区通行约束、车辆/工人容量、任务服务时长、时间窗、返回起点、天气、禁行区和多车辆 VRP。它解决的是单起点单旅行商近似问题，不是农场派工路径问题。
5. **2-opt 是启发式，不保证最优。** `routeOptimizer.ts:112-138` 固定 50 轮，且只在 2-opt 局部搜索；没有与精确解、其他算法或真实路线矩阵比较。文件头 `:6-7` 的节省 15% 只是目标，没有评估集或运行时校验。
6. **当前数据难以形成真实闭环。** `greenhouses` 表虽有 `lat/lng` 字段，但当前 21 个温室仅 9 个有有效坐标；51 个任务中只有 7 个关联到有效坐标。派工端点 `routes/dispatch.ts:135-167` 在找不到坐标或今日任务时会直接失败或返回空任务。

#### 评分理由

算法本身不是假实现，所以算法真实性给 2.0；但它没有 Service 内真实数据查询、约束和验证，业务完整性只有 1.5。返回结构可以被前端展示，但缺少路线约束、质量指标和原始顺序校验，给 0.7。

**结论：**可作为“最近邻 + 2-opt 的路径演示器”，不能作为真实派工优化 AI。

### 5.2 AI-07 资源优化

#### 已实现的内容

- 入口是 `POST /api/ai/resource/optimize`，`routes/ai/resource.ts:12-18`；路由装配在 `server/src/routes/index.ts:229`，且位于认证中间件之后。
- 实际查询 `inventory_stock` 当前库存，SQL 在 `resourceOptimizer.ts:62-77`。
- 实际查询 `inventory_transaction` 出库消耗，SQL 在 `resourceOptimizer.ts:84-92`。
- 按库存和平均日消耗计算剩余天数、告警等级，逻辑在 `resourceOptimizer.ts:94-107`。
- 计算建议采购量、估算成本、汇总和排序，逻辑在 `resourceOptimizer.ts:109-160`。

#### 主要不足与差距

1. **计划目标没有实现。** 文件头 `resourceOptimizer.ts:4-8` 声称要基于历史消耗、种植计划、季节性因素预测需求；`resourceOptimizer.ts:9-13` 自己也承认 V1 只使用库存和出库流水。没有种植计划表查询、季节性、趋势、回归或预测模型。
2. **状态过滤会主动漏告警。** `resourceOptimizer.ts:67` 固定 `status = 'in_stock'`。当前库有 3 行 `low_stock`、2 行 `empty`、7 行冻结状态，却不会被扫描，因此默认调用得到 47 种全部正常并不代表库存真实正常。
3. **使用了错误的库存口径。** 查询明明选了 `available_quantity`（`resourceOptimizer.ts:65`），计算却使用 `current_quantity`（`resourceOptimizer.ts:94`）。冻结数量不会从可用库存中扣除，可能把不可用库存当成可采购剩余量。
4. **按库存实例而非物料聚合。** `resourceOptimizer.ts:83-130` 对每一条 `inventory_stock` 单独计算，同一物料多个仓库、批次或实例会生成多条预警和重复成本。`total_materials` 实际是 in_stock 实例数，不是物料数。
5. **成本是硬编码假设。** `resourceOptimizer.ts:33`、`resourceOptimizer.ts:115`、`resourceOptimizer.ts:147` 固定按 10 元/kg 计算；没有 `unit_price` 查询、供应商报价、币种、单位换算或税费。当前库存和采收表存在多个单位，因此估算成本没有业务可靠性。
6. **采购建议没有真实采购闭环。** 只返回内存结果，不创建采购计划、不检查现有采购单、不处理交期和安全库存。推荐公式在 `resourceOptimizer.ts:109-114` 只是用日消耗乘一个固定窗口，没有服务水平、补货提前期或季节性。
7. **输入值和 SQL 拼接存在安全问题。** `resourceOptimizer.ts:68-69` 将 `material_name`、`warehouse_id` 直接插入 SQL 片段，路由 `resource.ts:12-15` 没有长度、字符或边界校验。这是可直接触发的 SQL 注入面，应改用参数化动态条件。
8. **healthy 口径和返回结构不完整。** `alerts` 只保留非正常记录（`resourceOptimizer.ts:117-130`），健康物料没有名称、仓库、库存、可用量和单位；`summary.ok` 只是 `stocks.length - alerts.length`，不是独立查询出的健康物料数。

#### 评分理由

查询确实来自两张真实库存表，所以数据真实查询给 1.5；但只查 in_stock、用 current_quantity 代替 available_quantity，导致业务结论可能错误。算法是固定规则，没有预测能力，算法真实性给 0.5。结构化结果能展示部分告警，但成本和库存口径不可靠，给 0.6。

**结论：**属于有真实数据来源的库存预警 MVP，不是资源需求预测或采购优化器。

### 5.3 AI-14 异常检测

#### 已实现的内容

- 入口是 `POST /api/ai/anomaly/detect`，`routes/ai/anomaly.ts:12-18`；路由装配在 `server/src/routes/index.ts:233`。
- 任务耗时基线和近期记录查询在 `anomaly.ts:50-94`，分组条件为每类任务至少 5 条历史记录。
- 产量基线和近期记录查询在 `anomaly.ts:96-138`，分组条件为每种作物至少 3 条记录。
- 出库流水和近期记录查询在 `anomaly.ts:140-187`。
- 缺勤率查询在 `anomaly.ts:189-224`。
- 返回异常、严重级别、建议动作和统计汇总在 `anomaly.ts:23-40`、`anomaly.ts:226-245`。

#### 主要不足与差距

1. **所谓 Z-score + IQR 实际不存在。** 文件头 `anomaly.ts:9-13` 声称使用 Z-score + IQR，但代码没有中位数、四分位数或 IQR。任务耗时用最大值和最小值估算 `std`（`anomaly.ts:69`），产量和库存用 `(max-min)/4`（`anomaly.ts:113`、`anomaly.ts:160`）。这不是标准差，只是范围近似。
2. **出勤维度根本没有 Z-score。** `anomaly.ts:207-218` 使用固定 20% 缺勤率阈值，并明确把 `z_score` 写成 0；`xai_reasons` 在 `anomaly.ts:231-237` 却统一声称 Z-score + IQR，属于结果说明与实现不符。
3. **按维度过滤没有生效。** 代码在 `anomaly.ts:50` 和 `anomaly.ts:97` 无条件执行任务耗时和产量；只有库存和出勤在 `anomaly.ts:143`、`anomaly.ts:192` 检查 `check_dimension`。因此请求 `inventory_change` 时仍会先查任务和产量，响应不响应调用者指定维度。
4. **计划中的环境维度未实现。** `AnomalyInput` 的可选维度只有任务、产量、库存、出勤和 all（`anomaly.ts:17-21`），没有环境数据查询，更没有传感器、阈值、告警去重或实时调度。
5. **基线不是日统计。** 任务、产量和库存的 mean/std 都是按记录计算，近期对比也是逐条扫描；没有按日期聚合。库存注释说是日均出库，但 SQL `anomaly.ts:144-168` 没有 `GROUP BY date`，同一天多条出库流水会被当成多个独立样本。
6. **样本和状态口径不严。** 历史任务未限制完成状态，产量未排除 `deleted_at`、审核状态或不同单位，近期查询没有排序和未来日期保护；库存也没有业务类型/单位归一化。稀疏分组会被完全跳过，而不是报告数据不足。
7. **无法证明质量指标。** 注释写检出率不低于 85%、误报率不高于 10%（`anomaly.ts:4-8`），但代码没有标注集、阈值校准、交叉验证、误报统计或结果置信度。`model_version` 只是字符串。
8. **当前数据库默认结果为空。** 当前 30 天窗口下任务、产量、库存和出勤四个基线分组均为空，因此实际 `total_checks=0`。这不是模型准确，而是没有满足最小样本条件，响应也没有返回 data-quality 状态。

#### 评分理由

查询确实覆盖四类真实业务表，所以数据真实查询给 1.5；但分组、最小样本、维度选择和日期口径均不足以支撑可靠的统计判断。算法真实性只给 1.0，业务完整性给 0.8。返回有异常结构和解释文字，但当前没有实际结果且解释包含失真的 IQR/Z-score 声明，给 0.5。

**结论：**是有真实 SQL 的统计规则原型，不能称为经验证的 Z-score + IQR 异常检测 AI。

### 5.4 AI-15 出勤异常

#### 已实现的内容

- 入口是 `POST /api/ai/attendance/detect`，`routes/ai/attendance.ts:12-18`；路由装配在 `server/src/routes/index.ts:234`。
- 按在职状态查询员工，支持班组过滤，SQL 在 `attendance.ts:55-75`。
- 读取员工最近考勤记录并统计连续缺勤、迟到、请假，逻辑在 `attendance.ts:78-114`。
- 用固定阈值生成连续缺勤、频繁迟到、请假异常和高缺勤率结果，见 `attendance.ts:116-163`。
- 返回员工、类型、严重级别、指标、动作和汇总，见 `attendance.ts:23-45`、`attendance.ts:179-186`。

#### 主要不足与差距

1. **`z_threshold` 参数是死参数。** `attendance.ts:51-52` 读取了 `z_threshold`，后面所有判定都没有使用它；实际阈值是 3、5、10、30、50 的硬编码。没有均值、标准差、历史基线或 Z-score 计算。`xai_reasons` 的 `attendance.ts:171-177` 仍声称滑动窗口 + Z-score。
2. **员工主表与考勤记录失配。** 员工查询只返回 `status IN ('在职','active')` 的 `employees`（`attendance.ts:55-67`），随后只查这些员工的 `attendance_records`。当前库 6 名 active 员工没有任何对应记录，所以默认 `total_employees=6` 且 `anomalies=[]`。考勤表中大量 `W001`、`S001` 等记录不会进入扫描。
3. **班组过滤在当前数据下也基本不可用。** `attendance.ts:58-62` 通过 `team_members.worker_id = employees.id` 联查；当前 `team_members` 中没有与 6 名 active 员工 ID 对应的成员，因此指定班组时会返回 0 名员工。
4. **没有排班日分母。** `attendance.ts:80-87` 使用 `LIMIT 30`，不是严格的日期窗口；`totalDays` 只是记录条数。员工没有记录会被当作 0% 缺勤，连续缺勤也只是连续记录，不检查日历日期和应出勤日。
5. **固定规则没有业务条件。** `leave_count >= 3` 就在 `attendance.ts:141-150` 标记请假异常，没有请假额度、审批状态、节假日、班组基准或岗位差异。迟到也没有晚到分钟数、严重程度或趋势判断。
6. **严重级别永远缺 low。** 接口声明 low/medium/high（`attendance.ts:23-27`），但所有分支的最低判定都是 medium 或 high；当前 `summary.low` 必然为 0，模型返回结构与实际规则不一致。
7. **结果可能重复。** 同一员工可以同时获得连续缺勤和高缺勤率等多个事件，服务没有去重、优先级或合并，也没有返回查询时间范围、异常记录 ID 或原始打卡时间。

#### 评分理由

有员工、班组和考勤表的真实查询，数据真实查询给 1.3；但员工 ID 对不上、班组联查为空、分母不是应出勤日，查询结果不具代表性。固定规则且 Z-score 参数未使用，算法真实性给 0.5，业务完整性给 0.7。结构化结果能展示事件，但当前默认没有有效异常且指标口径不完整，给 0.5。

**结论：**是固定阈值的考勤规则筛查器，不能作为统计异常评分器或经过验证的 AI 模块。

## 6. 横向真实性结论

### 已具备的真实部分

- 四个 API 路由确实已注册到 `/api/ai/*`，并且位于认证中间件之后，不是只在文件中存在但不可访问的壳。
- AI-08 的 Haversine、最近邻和 2-opt 代码确实可执行，能返回真实的 Haversine 距离和顺序。
- AI-07、AI-14、AI-15 确实执行了部分真实 SQLite 查询，不是完全硬编码返回。
- 前端 `aiApi.ts` 和 AIPanel 已经为四个模块定义了调用入口，路由结果经过统一 camelCase 中间件后可以被前端消费。

### 尚不能证明的部分

- 没有训练数据、训练代码、模型文件、交叉验证、误报/漏报评估或线上反馈闭环。
- 注释中的 15% 节省、85% 检出率、10% 误报率、80% 出勤异常检出率均没有实现证据。
- 当前默认数据下 AI-07、AI-14、AI-15 都返回 0 个业务事件；这应被视为数据不足或规则未命中的显式状态，而不是系统健康的证据。
- `model_version` 只是版本字符串，不能证明存在模型；IAPanel、modelVersion 等展示字段不能替代模型验证。

## 7. 优先级改进建议

### P0：先修正会误导业务判断的问题

1. 修正 `anomaly.ts` 的 IQR/Z-score 描述，移除不存在的算法声明；按维度真正短路查询，并在没有最小样本时返回 `data_quality=insufficient`。
2. 修正 `attendance.ts` 未使用的 `z_threshold`、无排班日分母、员工主数据关联和 low 级别永不为 0 的问题。
3. 修正 `resourceOptimizer.ts` 的状态过滤、`available_quantity` 口径、参数化 SQL 和单位成本来源；没有真实单价时不要输出伪精确成本。
4. 为 `routeOptimizer.ts` 校验经纬度、任务 ID、原始顺序和数量上限，并在响应中明确是直线距离估算而非道路导航距离。

### P1：形成可验证的业务闭环

1. 路径模块接入统一任务/温室坐标契约，记录数据来源和坐标缺失率；按任务服务时间、容量、时间窗和返程规则建立评估样本。
2. 资源模块先按物料主键 + 仓库聚合，查询可承诺库存、有效单价、采购单和补货提前期，再实现季节性/趋势预测。
3. 异常模块先建立带标签的校准集，再谈检出率、误报率和阈值；按日期聚合输入，避免把流水条数当样本量。
4. 出勤模块基于排班表计算应出勤日，结合请假额度、审批状态、迟到分钟数和班组/岗位基线，不能只看记录数。

### P2：补齐验证和工程保障

- 为四个模块补充单元测试、集成测试和端到端测试，至少覆盖空数据、稀疏数据、边界输入、非法顺序、冻结库存、员工无考勤、班组为空和数据库缺列。
- 每次返回增加 `generated_at`、`query_window`、`data_quality`、`rules_applied`、`sample_size` 和 `limitations`，让前端能区分无数据、无异常和无权限。
- 建立模型/规则版本变更日志和离线评估报告；没有评估报告时，不得在界面上使用 AI 准确率或检出率表述。

## 8. 最终诚实结论

当前四个模块不应被描述为已完成的 AI 生产能力：

- AI-08 是可运行但过于简化的 VRP 启发式演示器，算法真实性最高，业务数据只由外部入口间接提供。
- AI-07 是真实库存查询加固定规则告警，不是需求预测或采购优化。
- AI-14 是多表统计规则原型，IQR 不存在，部分维度声称与实现不符，当前默认数据没有可用检查组。
- AI-15 是固定阈值考勤筛查，Z-score 参数未使用，员工数据关联和排班分母均未闭环。

因此本次审核结论为：**四个模块均有部分真实代码和真实数据链路，但整体真实度偏低；当前版本可作为 MVP 演示，不具备宣称生产级 AI 准确率或业务决策可靠性的证据。**
